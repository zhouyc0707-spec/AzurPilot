# 旧版统计页 → React 新前端 整页还原：对照清单与实施计划

> 目标：在 React 新前端（`frontend/`）中还原本地旧版 PyWebIO 统计页的面板、字段与交互。
> 验收标准（用户确认）：**面板、字段、交互、图表行为一致；细节间距允许微调**。
>
> 生成时间：2026-09-24（阶段 1 交付物）

## 一、旧版统计页构成（还原目标）

页面外壳：`module/webui/app_statistics_page.py`（约 9.6 KB）

| 面板 | 实现文件 | 体积 | 图表脚本 |
|---|---|---|---|
| 资源概览仪表盘（sticky、横向卡片、隐藏舰队币） | `app_stat_resource.py` | 7.4 KB | — |
| 资源变化趋势（日/周/月视图、K 线、缩放/重置、明细切换） | 同上 | | `webapp/resource_chart.js` 27.5 KB + `resource_chart.html` |
| 体力/行动力变化（24h/周/月、平均行动力、月度净额/购买/出击消耗） | `app_stat_action_point.py` + `app_stat_action_point_toolbar.py` | 32.2 + 4.3 KB | `webapp/ap_chart.js` 48.8 KB + `ap_chart_panel.html` |
| 大世界侵蚀一数据收集表 | `app_stat_opsi.py` | 24.6 KB | — |
| 短猫相接数据收集表 + 本月收获 + 历史月份 | `app_stat_opsi.py` / `app_stat_opsi_export.py` | 24.6 / 11.1 KB | — |
| 委托收益统计（卡片统计 / 日周月 / 明细 / 最近记录分页 / 正在进行 / 查看截图） | `app_stat_commission.py` | 22.4 KB | — |
| 舰船经验与每日经验检测（升级进度表 + 每日经验/战斗/运行序列） | `app_stat_ship.py` | 6.4 KB | — |

### 字段级清单（由旧页 i18n 键枚举，共 100 个 `Gui.Stat.*` 键）

- **资源**：ResourceChartTitle / DetailChartTitle / ToggleDetailChart / ResetChart / Refresh /
  ViewDayButton·ViewLineButton·ViewMonthButton / ViewTitleDay·Line·Month / ViewLabel /
  CannotAggregateKline / NewDataAvailable / NoResourceData / NoValidResourceData / LoadResourceDataFailed
- **行动力**：ApPeriodLast24h / ApPeriodWeek / ApPeriodMonth / AverageAP / MonthlyNetAP /
  MonthlyPurchasedAP / MonthlySortieCost / SortieCost / MonthlyLoopEfficiency /
  NoApData / NoValidApData / LoadApDataFailed
- **大世界（侵蚀一）**：OpsiDataCollectionTitle / BattleCount / BattleRounds / BattlesCompleted /
  AkashiEncounters / AkashiRate / SirenResearchDevices / SirenResearchRate / HazardLevel / Month /
  CheckTime / LastCheckTime / EstimatedTime / AvgRoundTime / AvgBattleTimeHeader / LoadOpsiStatsFailed /
  NoTodayBattleData / HourUnit·MinuteUnit·SecondUnit
- **短猫相接**：AvgMeowRoundTime / AvgMeowRoundTime(平均一轮时长) / MeowEffectiveRounds /
  MeowAvgOperationCoin / MeowAvgPlate / MeowAvgAbyssal / MeowAvgObscure / MeowLastRecord
- **委托收益**：CommissionIncomeTitle / CardStat / Day·Week·Month / TotalCommissions /
  CommissionIncomeItemGem·Cube·Chip·Oil·Coin / CommissionIncomeNoData /
  CommissionIncomeRecentTitle / RunningCommissionTitle·ScannedAt·Finish·None·Unknown / ViewScreenshot
- **舰船经验/每日经验检测**：ShipExpProgressTitle / DailyExpCheckTitle / Level / TargetLevel /
  CurrentExpThisLevel / TargetExpRequired / ExpToTarget / TotalExp / TodayExp / TodayRun /
  ExpEfficiency / ExpRemaining / ShipSlot / SortiesNeeded / LoadShipExpFailed /
  LoadExpStatsFailed / NoShipExpData / NoExpData

## 二、新前端现状（`frontend/src/pages/Statistics.tsx` + `module/api/statistics_service.py`）

新统计页是**通用渲染模型**：后端 `statistics.report` 按分类返回 `metrics / series / tables / notes`，
前端用 `StatisticsTable.tsx` + `StatisticsChart.tsx` 统一渲染。六个分类：

| 新分类 | 后端已产出的内容 |
|---|---|
| `resources` | 资源序列（`series`） |
| `action` | 行动力 / 行动力资产 / 海里数 / 作战补给凭证 / 特别兑换凭证 五条序列 |
| `opsi` | 指标 + 「短猫运行统计」表（侵蚀等级 / 战斗次数 / 有效轮数 / 平均战斗秒数 / 平均每轮秒数 / 研究装置 / 获取率 / 统计来源） |
| `commission` | 指标 + 「委托收益明细」表 + 「委托结算记录」表 + 五条物品序列 |
| `ships` | 目标等级 / 经验效率 / 平均战斗时长 / 平均每轮时长 / 短猫平均战斗时长 / 今日战斗 / 今日经验 / 今日运行 + 「舰船升级进度」表 + 三条每日序列 |
| `loot` | 掉落明细（旧版无此分类） |

图表能力（新前端已有）：折线 / 蜡烛（K 线）/ 叠加线、坐标轴统一/分离、时间桶（5 分钟 / 小时 / 日）、
搜索表格、导出、图表折叠。

## 三、差异清单（要补什么）

| # | 旧版功能 | 新前端现状 | 结论 |
|---|---|---|---|
| 1 | 资源趋势（日/周/月 + K 线 + 缩放重置 + 明细） | 有图表与 K 线/叠加线能力 | **基本已有**，需核对视图切换与"重置"行为 |
| 2 | 行动力图表（24h/周/月） | 有 5 条序列 | **基本已有**，需补月末指标？ |
| 3 | 月度行动力指标（净行动力 / 购买 / 出击消耗 / 循环效率） | 后端 `opsi` 分类有 `compute_monthly_cl1_akashi_ap` 等 | **待核对**是否有对应指标 |
| 4 | 侵蚀一数据收集表（明石遇见 / 研究装置 / 获取率 / 检查时间 / 预估时间） | 只有「短猫运行统计」一张表 | **部分缺失**：侵蚀一表与检查时间/预估时间需补 |
| 5 | 短猫**本月收获**表 + 查看历史月份 | 前端未见 `Monthly/本月/月度` | **确认缺失**（后端 `opsi_month` 有数据方法） |
| 6 | 委托收益「正在进行」区块 | 前端未见「正在进行」 | **确认缺失**（后端有 running 记录） |
| 7 | 委托收益「最近记录」分页 | 有「委托结算记录」表（是否分页待核对） | **部分缺失** |
| 8 | 委托收益**查看截图** | 前端 `commission_rewards` 命中 0、后端 `statistics_service` 无 `screenshot` 字段 | **确认缺失**（需后端截图字段 + 静态路由 + 前端按钮） |
| 9 | 每日经验检测（标题 + 今日战斗/经验/运行合并行） | `ships` 分类已有对应指标 | **基本已有**，需核对标题与合并行样式 |
| 10 | 资源概览 sticky 仪表盘（隐藏舰队币、仅 8 项） | 新前端有 `ResourceCards` 组件 | **待核对**是否同款字段 |
| 11 | 面板顺序与版式（旧版单页多面板） | 新前端为分类切换 | **需按旧版重组页面** |

## 四、实施计划

- **阶段 2 后端接口**：为旧面板补专用接口（不复用通用 `statistics.report` 表格模型）
  - `commission_rewards` 截图静态路由（挂到 `module/api/static.py` 或路由表）
  - 委托结算记录补 `screenshots` 字段；「正在进行」列表接口
  - 短猫月度收获接口（`opsi_month`，含可用月份列表）
  - 侵蚀一统计表接口（含检查时间 / 预估时间 / 明石列）
- **阶段 3 页面骨架**：在 `Statistics.tsx` 下新增「旧版视图」，按旧版面板顺序与标题组织
- **阶段 4 图表复用**：把 `webapp/ap_chart.js`、`webapp/resource_chart.js` 包成 React 组件
  （同一套绘制函数 + `useEffect` 挂载，保留缩放/重置/resize 行为）
- **阶段 5 表格与分页**：侵蚀一表（列顺序按本地定制）、短猫本月收获（含历史月份切换）、委托最近记录分页
- **阶段 6 截图查看**：静态路由 + 行内「查看截图 N」
- **阶段 7 构建与实机验收**：`npm run build --prefix frontend`，用 `ALAS_WEBUI=react` 起实例逐项对照旧版

## 五、待确认 / 风险

- 新前端的通用 `Statistics.tsx` 是上游持续演进的页面，**新增「旧版视图」应独立成分支/路由**，避免与上游每次合入都冲突（与保留旧界面的分叉策略一致）
- `frontend/` 需要 Node 构建（本机 Node v24 已具备；`npm.ps1` 受 PowerShell 执行策略限制，Python 子进程走 `npm.cmd` 不受影响）
- 阶段 4 复用旧 JS 时需确认其依赖（jQuery/MutationObserver 等）在新页面环境下可用
