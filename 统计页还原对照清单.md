# 旧版统计页 → React 新前端 整页还原：对照清单与实施记录

> 目标：在 React 新前端（`frontend/`）中还原本地旧版 PyWebIO 统计页的面板、字段与交互。
>
> 验收标准（用户确认）：**面板、字段、交互、图表行为一致；细节间距允许微调**。
>
> 生效范围（用户确认）：**旧版主题（`legacy-light` / `legacy-dark`）下整页还原**，
> 其它主题保留上游的分类视图 —— 与上游「旧版主题还原旧 WebUI 版式」的做法一致。
>
> 状态：**已实现并实测**（2026-09-24）。实现清单见 `定制化修改清单.md`
> 第一章「旧版统计页整页还原（React 旧版主题）」。

## 一、目标口径的修正（读代码后发现的偏差）

「旧版统计页」指**本 fork 定制过的**旧界面，不是上游原始版本。权威依据是
`定制化修改清单.md` 第一章与 `module/webui/app_stat_*.py` 的当前代码。最初清点时
有四处判断不准，这里按代码修正：

- **全资源变化趋势图表整体隐藏**：旧页面里没有这张图 —— `app_statistics_page.py`
  里的 `put_scope("resource_chart")` 与它的周期刷新都被注释掉了
  （定制清单「全资源变化趋势图表」）。React 版**不渲染**该面板。
- **体力变化图表没有「日/周/月 + K 线」视图**：`_render_ap_chart_toolbar` 全仓库没有
  调用点（死代码），K 线聚合算法虽在 `ap_chart.js` 里但入口已丢失；实际可见的是
  折线 + 时间范围按钮（近24小时 / 近七天 / 本月，默认近七天）。
- **委托收益没有明细表格**：「委托次数 / 平均每次」已并进五张物品卡片的第三行。
- **`app_stat_opsi.py` 里的「每日经验检测」不可见**：它先渲染进 `opsi_stats` 作用域，
  紧接着被 `_render_opsi_summary(clear=True)` 清掉；页面上显示的是
  `app_stat_ship.py` 的「每日经验检测：识别到的舰娘等级与升级进度」。

## 二、面板对照与实现状态

| # | 旧面板（标题） | 旧实现 | React 实现 | 状态 |
|---|---|---|---|---|
| 1 | 资源仪表盘（8 项） | `app_stat_resource.py` + `app_dashboard.py` | `components/ResourceCards.tsx`（`StatisticsLegacy` 内，嵌在总览页时跳过） | ✅ |
| 2 | 体力变化 - {范围} | `app_stat_action_point.py` + `webapp/ap_chart.js` | `components/LegacyApChart.tsx`（ECharts） | ✅ |
| 3 | 雪风大人的大世界数据收集 | `app_stat_opsi.py` | `pages/StatisticsLegacy.tsx` | ✅ |
| 4 | 本月 / 历史耄耋相接收获 | `app_stat_opsi_export.py` | 同上（含历史月份选择器） | ✅ |
| 5 | 每日经验检测（舰船升级进度） | `app_stat_ship.py` | 同上 | ✅ |
| 6 | 委托收益统计 | `app_stat_commission.py` | 同上 | ✅ |
| — | 全资源变化趋势 | 旧页面已隐藏 | 不渲染 | ✅（按旧页面） |

数据层：新增 `statistics.legacy`（`module/api/legacy_stats_service.py` +
`module/api/router.py` + `module/api/protocol.py`），一次返回六个面板的数据；
只给数据与 i18n 键，文案由前端用既有 `Gui.Stat.*` 翻译渲染，**不新增前端 i18n 键**。

## 三、逐面板口径（日后核对用）

- **资源仪表盘**：固定 8 项 石油/物资/钻石/活动PT/魔方/核心数据/勋章/功勋
  （行动力/黄币/紫币/舰队币不显示），窄屏吸顶。
- **体力变化图表**：默认近七天、默认只画体力（旧 `seriesVisible = [true,false,false,false,false]`）；
  主曲线按涨跌分段着色（涨红跌绿、持平灰）；黄币/紫币/海里数/资产共用右侧一条 Y 轴
  （刻度带千位分隔符）；紫币只画大于 0 的点；滚轮缩放 + 拖拽平移 + 「重置图表」；
  配色照抄 `module/webui/ap_chart_theme.py` 的深浅两套。
- **大世界数据收集**：12 列 × 3 行，行序 1 → 5 → 3；出击轮次展示取整、出击消耗按
  **未取整**的有效轮次算（侵蚀1 每轮 5、侵蚀3 每轮 15、侵蚀5 每轮 30）；概率分母是
  轮次、平均体力 = 购买行动力 ÷ 明石次数（四舍五入取整）；汇总行四条只在数值上着
  正负色（正 `#d32f2f` / 负 `#00897b`，深色主题 `#ef5350` / `#26a69a`）。
- **耄耋相接收获**：14 列 × 2 行（侵蚀 3 / 5）；前 9 列随所选月份变化，后 5 列
  （历月有效战斗轮数总和 + 四个平均）来自累计 CSV、不随月份变化；平均值保留 6 位小数；
  「上次记录时间」取累计表里最新的时间戳；历史月份选择器最多 24 个月，没有历史月份时
  提示「暂无历史月份数据」。
- **舰船经验**：9 列；「已战斗场次」取今日 `battle_count`；汇总一行 = 上次检查时间 /
  今日经验 / 经验效率（`/小时`）/ 今日运行（`分钟`）；无今日数据时另给一条弱提示；
  完全没有数据时显示「暂无舰船经验数据…」。
- **委托收益**：今日 / 本周 / 本月三套卡片一次算好（区间口径沿用旧界面的
  `get_commission_income_summary`，只读当前自然月桶）；卡片显示
  `+总量`（总量为 0 时显示 `0`）与 `{count}次 · 平均/次 {avg}`；「正在进行」读
  `log/commission_running_<实例>.json`（钻石委托金边、标题旁「上次扫描 HH:MM」，
  未扫描过 / 无运行中委托各有文案）；最近记录 10 条一页、最多 5 页，记录形如
  「时间 + 物品胶囊 + 查看截图」，只显示被统计的五种资源且数量 > 0；
  脚注「委托总次数: N」。

## 四、与旧界面的有意差异

1. 各面板标题旁的刷新图标统一触发整页数据重取（后端一次返回全部面板数据），
   另有 60 秒静默自刷新；旧界面是各板块分别重绘 + 15 秒「有新数据可用」提示
   （该提示未还原）。
2. 委托卡片图标改用新前端的 webp 图标（旧界面用 `assets/gui/icon/icon_N.png`，
   新 API 不暴露旧资源目录），语义色保持一致。
3. 体力图表概览行的「(现值) 总量」只显示总量：生产库快照里没有 `ap_now` 字段
   （旧界面此时显示的是 `(0)`）。
4. 侵蚀 3/5 行「平均一轮时长」的列头与取值统一用 `AvgRoundTime`：旧界面取值走
   `AvgMeowRoundTime`，两键在英文下文案不同，导致英文界面该列恒为占位符。
5. 图例与概览行文案沿用旧界面的硬编码中文（体力/紫币/黄币/资产/海里数）与
   耄耋收获表的六个中文列名（金菜/彩图纸/金机密/隐秘/深渊/金猫箱）。

## 五、验证记录

- **后端回归**：`tests/test_legacy_stats_service.py`（13 项）锁住行序、取整、占比分母、
  正负着色、占位符、紫币过滤与「一条记录一张截图」。
- **全量单测**：`python -m unittest discover -s tests` → 1431 项，失败集合与改动前一致
  （4 项环境/顺序相关 + 5 项顺序污染的 `test_webui_opsi_rounds_display`，单独运行 7/7 通过）。
- **构建**：`npm run typecheck --prefix frontend` 与 `npm run build --prefix frontend` 通过。
- **浏览器实测**（`ALAS_WEBUI=react` 预览实例 + 系统 Edge / Playwright）：
  面板标题五个与旧页面逐字一致；三张表的列数与行数正确（12×3 / 14×2 / 9×3）；
  委托 5 张卡片、正在进行 4 条（含 1 条钻石委托金边）、最近记录 10 行 + 「1..5」分页
  （翻页后首行时间变化）、10 个「查看截图」链接指向
  `/static/commission_rewards/<实例>/<月份>/<文件>`；体力图表默认只有体力选中、
  切「近24小时」高亮生效、「重置图表」回到近七天且只留体力；无历史月份时点
  「查看历史月份」提示「暂无历史月份数据」；控制台无报错。

## 六、合入上游时的注意点

- 上游对 `frontend/src/pages/Statistics.tsx`、`module/api/statistics_service.py`
  的改动照常合入；本地只在 `Statistics.tsx` 里加了一行 `legacy` 分支与懒加载，
  改动面刻意收在最小。
- 新增文件（`StatisticsLegacy.tsx`、`LegacyApChart.tsx`、`legacy_stats_service.py`、
  两个 CSS）不与上游同名，正常不会冲突。
- 旧界面的口径若变动（`module/webui/app_stat_*.py`），必须同步
  `module/api/legacy_stats_service.py` 与前端渲染，两边一起改。
