# AzurPilot 本地定制化修改清单

> 本文件记录本仓库（`zhouyc0707-spec/AzurPilot`）相对上游
> `wess09/AzurPilot` master 分支的所有本地定制修改。
>
> 对比方式：`git diff wess09/master master`
>
> 最后更新：2026-09-13

## 一、WebUI 界面定制

### 1. 概览页布局

- 调度器列与右侧统计面板之间增加 `1rem` 列间距，避免两侧挤在一起。

### 2. 日志页与统计页位置互换

- 原右侧日志区改为统计图表面板，日志单独作为页签/按钮打开。

### 3. 统计页顶部「资源概览」仪表盘

- 从日志页移至统计页顶部。
- 横向排列，并增加边框。
- 使用 `sticky` 冻结在顶部，滚动时保持可见。
- 去除圆角，避免冻结时四个角露出底部内容。
- 隐藏舰队币。
- 仅显示：石油、物资、钻石、活动PT、魔方、核心数据、勋章、功勋
  （行动力/黄币/紫币不重复显示，避免与下方体力图表重复）。

### 4. 体力变化图表

- 上方「行动力/黄币/紫币/海里数/资产 变化 最高 最低」描述字号
  由 12px 调整为 14px。
  （原「五列网格统一为 150px / 110px / 100px / 100px / 90px」的定制
  已不适用于当前实现：序列概览行改为 flex + `gap:2px 20px` 自动换行，
  不再使用固定列宽。）
- 缩小右侧刻度预留宽度（110 → 72），消除图表右侧空白。
- 隐藏 `+` / `-` 缩放按钮（面板模板中已无对应按钮元素，仅保留「重置」；
  `ap_chart.js` 中读取 `_zoom_in` / `_zoom_out` 的监听代码因取不到元素
  而自动跳过），重置按钮移至右下角并下移，避免遮挡底部时间坐标。
- 曲线与坐标配色跟随 WebUI 主题（见第 15 条），主曲线按涨跌分段着色。
- 默认时间范围为「近七天」（见第 14 条）。

### 5. 大世界侵蚀1统计表（雪风大人的数据收集）

- 删除「今日战斗 / 今日经验 / 今日运行时间」三列。
- 将「塞壬研究装置 / 塞壬研究率」（吊机次数 / 吊机概率）两列
  从循环效率之前移到「循环效率」与「经验效率」之间。

### 6. 短猫相接收获区域

- 删除独立的「刷新」按钮。

### 7. 全资源变化趋势图表

- 整体隐藏，不再渲染。

### 8. 总览页统计面板异步渲染

- 进入总览页或来回切换页面时，统计视图（资源概览、体力图表、
  大世界统计、舰船经验、委托收益）不再同步渲染，改为只挂载空容器，
  由后台任务线程立即填充内容，消除页面切换的卡顿。

### 9. 统计界面标题字号统一

- 「体力变化 / 雪风大人的侵蚀一数据收集 / 耄耋相接数据收集 /
  耄耋相接收获 / 每日经验检测 / 委托收益统计」等标题统一为
  `1rem`，行动力/黄币/紫币/海里数/资产五行统计文字放大至 `14px`。

### 10. 委托收益最近记录分页

- 委托收益统计标题字重统一为 `600`，与其余统计标题一致。
- 最近委托记录加载最近 50 条，单页展示 10 条，支持上一页/下一页
  翻页、1~5 页码按钮跳转与页码显示，分页控件字号统一缩小。

### 11. 委托收益截图查看

- 委托结算时，把「获取物品」弹窗截图落盘存档到
  `log/commission_rewards/<实例>/<YYYY-MM>/`，路径写入委托收益
  数据库记录。
- 统计页「最近委托记录」每条有截图的记录行内显示「查看截图」按钮，
  点击在新标签页单独打开截图（多张截图依次显示「查看截图2」等）。
- 截图仅保留最近 50 张（与统计页 50 条最近委托记录对应），
  超出部分在每次保存截图后自动清理。
- WebUI 新增静态路由 `/static/commission_rewards` 挂载截图目录，
  仅暴露截图文件，不暴露日志等其余内容。
- 旧版本产生的历史记录没有截图字段，不显示按钮，自然降级。

### 12. 每日经验检测统计行合并与耄耋相接标题精简

- 「每日经验检测：识别到的舰娘等级与升级进度」下不再显示
  「平均战斗时间」「平均一轮侵蚀1时长」，效率与今日统计合并为
  一行：「今日战斗 / 今日经验 / 经验效率 / 今日运行」。
- 耄耋相接数据收集表格「平均一轮耄耋相接时长」列标题精简为
  「平均一轮时长」（五种语言同步更新）。

### 13. 统计图表窗口缩放自适应

- 体力变化图表与资源变化趋势图表的 Canvas 此前只在加载时读取一次
  尺寸，浏览器窗口缩放后图表不会跟随变化。
- 修复：窗口 resize 时防抖 150ms 重绘；画布宽度恢复 CSS 百分比
  宽度后再读取布局宽度（原实现会把宽度钉死成像素值，导致重绘
  也取不到新宽度）；重绘前先移除旧监听器避免重复绑定；缩放/
  平移状态提升到闭包层，缩放后调整窗口大小仍保持当前视图。
  用 Node DOM 模拟冒烟测试验证：体力线图 800→1200→600、K 线
  800→1000、资源图 800→1100 均正确重绘。

### 14. 统计界面两区域布局与体力图表时间范围

- 统计内容拆成两个区域：上方资源仪表盘固定不动，下方图表区为独立
  圆角卡片并自行滚动，滚动只发生在图表区，仪表盘始终可见
  （`#pywebio-scope-stat_panels` 用 `grid-template-rows: auto minmax(0,1fr)`
  实现；窄屏 `max-width:768px` 时仪表盘改为 `position:sticky` 吸顶）。
  总览页与独立统计页共用同一套面板。
- 体力变化图表新增「今日 / 近七天 / 本月」时间范围按钮，放在图例
  「─体力」左侧：今日取当天、近七天为含今天在内的滚动 7 天窗口、
  本月取当月全部。辅助序列（黄币/紫币/资产/海里数）用同一组时间戳
  过滤后按图表点对齐，不会错位。
- **默认显示近七天**（`_AP_DEFAULT_PERIOD = "week"`）：整月点数太多、
  细节被压扁；点「本月」仍可看整月。
- 按钮文案与语义对齐：用独立的 i18n 键 `ApPeriodDay` / `ApPeriodWeek` /
  `ApPeriodMonth`（今日 / 近七天 / 本月，五种语言），不借用委托收益的
  「本周」（那是自然周口径）。
- 标题与图例的留白收紧（标题行上边距 16→10px、标题下边距 8→6px、
  图例下边距 8→6px），缓解标题到图表面板之间的空白。

### 15. 体力图表主题适配与涨跌着色

- 图表画布读取不到 CSS 变量，配色由 Python 侧按当前 WebUI 主题注入
  （`module/webui/ap_chart_theme.py`）：浅色主题族（default、
  advanced_material）用白底 + 深一档序列色，深色主题族（dark、
  dark_advanced_material）沿用深底配色。
- 主曲线改为**按涨跌逐段着色：上升段红色、下降段绿色**，四个主题一致
  （此前浅色主题走的是「主题色单线 + 面积渐变」，整条线只有一种颜色，
  看不出体力在涨还是在掉）；点稀疏时补画的数据点也跟随涨跌。
- 浅色主题的涨跌色取更深一档（`#d32f2f` / `#00897b`）保证白底对比度；
  主色 `ap_line` 与上升色 `inc` 对齐，图例色块与曲线颜色一致；
  tooltip 里的体力数值改用常规文字色，不再借用曲线色。

### 16. 委托收益统计改版

- 「委托次数 / 平均/次」原先是独立表格，现并入获得数量下方的五张
  物品卡片（`{count}次 · 平均/次 {avg}`，五种语言），删除原表格。
- 刷新按钮改为**标题「委托收益统计」右侧的圆环箭头图标按钮**
  （图标以 SVG data URI 作按钮背景图，深浅两套按主题切换；标题行用
  `.stat-title-row` 排布，标题过长时省略号截断，图标不参与收缩）。
- 去掉最近委托记录分页控件上重复的「第 x / y 页」文字（按钮组已高亮
  当前页）。
- 「每日经验检测：识别到的舰娘等级与升级进度」的刷新按钮同样改成标题
  旁的图标按钮；图标实现抽到共用模块 `module/webui/stat_icon.py`。

### 17. 统计界面按钮样式统一

- 统计页所有按钮统一为「中性描边 + 选中主色实心」：常态描边圆角矩形，
  选中态主色填充 + 反色文字，悬停浅主色底 + 上浮 1px，按下缩放 0.97。
  分段按钮（今日/近七天/本月、分页、月份选择）合并为一组、共享外框，
  组内按钮保留各自底色使分段边界可见。
- 样式统一放在 `assets/gui/css/entry-alas.css` 的「统计页统一按钮样式」
  区块（不再由 Python 运行时注入，避免刷新后丢失）；作用域限定在
  统计各区域。
- 修掉两处布局问题：分段容器按基线对齐导致按钮组整体下沉
  （改用 `align-items:center` + `vertical-align:middle`，并清掉主题
  `.btn-off` 残留的 2px 外边距）；按钮被 `alas-mobile.css` 的
  `width:100%!important` 拉成整行宽（统一样式里显式钉住按内容取宽，
  并固定高度/内边距/行高抵消移动端覆盖）。

### 18. 统计界面板块间距统一

- 各板块标题的上边距原先各写各的（12px / 20px / 内联 20px 混用），
  「每日经验检测」与「委托收益统计」看着贴得太近。现统一由
  `.stat-section` 收口为**上边距 24px / 下边距 8px**，样式随面板注入
  一次；标题构造改用 `build_stat_section_title()`，调用处不再各传
  margin 参数；带图标按钮的标题行也在 CSS 里对齐到同样间距。
- 以后调整全局间距只需改 `module/webui/app_helpers.py` 的
  `STAT_SECTION_STYLE` 一处。

### 19. 移除耄耋相接收获下方的刷新与导出按钮

- 「耄耋相接收获」区块下方的「刷新」与「导出并保存到桌面」（导出 CSV
  到桌面）两个按钮已无保留价值，按钮与其回调一并删除；失去调用方的
  `_export_opsi_csv`（约 120 行）与相关导入同步清理，模块职责收敛为
  纯月度收获视图。
- `CsvFilenameTemplate` / `SavedToDesktop` 等导出相关翻译键保留未删
  （无入口但不影响使用，便于日后把导出改到别处）。

### 20. 修复板块因 NameError 消失

- 现象：统一板块间距后，「本月耄耋相接收获」与其后的「耄耋相接数据
  收集」两个板块从页面上消失。
- 根因：`app_stat_opsi_export.py` 的导入换成了 `build_stat_section_title`，
  但月份标题那一处仍在调用旧的 `build_title_block`，`_render_monthly_meow_loot`
  抛 `NameError`；渲染异常被上层捕获成日志，页面表现为板块凭空不见。
- 修复：补回 `build_title_block` 导入。
- 新增 `tests/test_webui_undefined_names.py`：静态扫描 `module/webui`
  下每个模块，凡是「用了但没定义也没导入」的名字都报错。这类错误导入期
  发现不了，只在具体渲染分支执行时才炸（已用故意移除导入的方式验证该
  测试确实能抓到）。

## 二、岛屿计划

### 1. 修复餐品因材料不足无限循环制作

- 餐品原料不足且游戏 UI 拒绝生产时，标记为「本轮不可用」，
  后续扫描直接跳过，避免同一 run 内反复无效点击。

### 2. 修复最低保留数量排产时岗位全放满

- 每个未达标作物最多只分配一个岗位，避免单个作物被重复分配到
  所有空闲岗位（例如香蕉缺 1 个却四个岗位全种香蕉）。

## 三、调度与停止

### 1. 岛屿任务统一对齐到半点触发

- 所有岛屿任务自动推迟到下一个 `XX:00` 或 `XX:30` 边界，
  降低调度频次。

### 2. 停止按钮状态卡顿修复

- 收尾动作改为后台线程回收，不再阻塞「停止 → 开始」按钮状态刷新。

## 四、战役 / 低耗

### 1. 紧急委托与三油低耗复用舰船装备

- 初始旗舰等级检查标志改为类属性持久化，任务切换后再回来时
  不重复执行换旗舰 / 换装备。

### 2. 修复主线打捞误读活动 PT 为 1

- 主线打捞（GemsFarming）在普通战役页不读取活动 PT，
  避免把页面其他数字误识别成 1。

### 3. 委托任务舰船掉落检测开关

- 新增 `Commission.DetectShipDrop` 配置项（默认关闭），控制领取委托
  奖励后是否检测「获得舰船」画面；不做会掉落舰船的委托时可关闭，
  避免识别错误。其他场景（战斗结算等）的舰船检测不受影响。

### 4. 委托收益数量识别修复（图标碎片过滤）

- 委托奖励截图中物品图标底部会伸入数量 OCR 区域，其白色纹理被
  OCR 误读为数字（如心智单元 11 被识别成 211）。
- 数量 OCR 增加连通域过滤：删除「远离数字主体」的孤立图标碎块，
  保留靠近数字的小组件（如 7 的顶横等被拆分的字形部件）；默认
  **只删除碎片像素、保留中灰像素**，避免抹掉字形边缘导致
  77→27、72→2 类误读；并在委托收入识别路径关闭 `crop_to_text`
  裁剪，避免文字位置变化导致误读（如金币 71 被读成 2）；
  战斗掉落统计路径保持原行为不变。
- 首轮读数超过物品上限时，换用「抹灰版」图像（非保留像素全部置白）
  兜底重试，消除靠近数字的图标残影误读（如残影被读成 S 使
  18 → 518 → 截断成 51）；抹灰版基于预处理结果（含子类缩放）构建；
  上限校验表补充模板名别名（`CognitiveChips`/`Gems`/`Cubes`/
  `Coins`/`OrdnanceTestingReportT4`）作为最终防线。
- 自律寻敌奖励页的数量 OCR 同样启用碎片过滤（数量区域放大 2.67 倍
  后识别，阈值按放大域校准为高 15/面积 30），修复特别兑换券
  3 被图标残影误读成 23 的问题；军械测试报告 T4 的 1 被读成 51
  由上限校验 + 抹灰版兜底修正。
- 奖励页数量 OCR 增加「右侧数字簇」规则：从最右数字出发按水平
  间隙（阈值 10px，数字间实测 ≤4px）传递聚类，簇外的图标笔触
  删除，修复图标竖笔画被误读成前缀数字（2→12、3→73 等）；
  民用电子元件补充数量上限（50）触发抹灰兜底。
- 碎片过滤增加「左侧横条」规则：悬在数字本体左侧、无水平重叠且
  面积 ≥20 的扁平横条按图标残影删除（修复数字 1 左侧的横条使
  1 被拼读成 7，如舰炮设计图 T5 的 ×7 实际为 ×1）；面积很小的
  像素斑点（衬线等抗锯齿部件）即使紧贴数字也保留。
- 用 50 张真实结算截图回归验证：211/27/2/51 等误读全部转为正确值，
  其余截图识别结果与数据库一致。
- 委托数量 OCR 改为「碎片过滤 + 2 倍放大 + 裁剪」：原尺寸下不裁剪
  会把右缘被截断的数字丢掉（71 → 7），裁剪又会丢掉 77 的末位
  （77 → 7）；放大 2 倍后两种读数全部正确（用 50 张留存委托截图
  回归，49 张与数据库一致，仅修正了此前 71 被记成 7 的那条记录）。
- 数量 OCR 首轮读数为 0 时，用**未裁剪**图像重试一次：数量区域右
  边界可能正好切掉数字（如防空装备设计图 T4 的「1」贴在格子边缘），
  `crop_to_text` 会再削掉一部分笔画导致读数 0，而 0 是非法数量
  （会抛 `ZeroAmountError` 使整次结算作废），因此该重试无代价。

## 五、大世界（侵蚀1 / 耄耋相接 / 明石）

### 1. 侵蚀一练级事件检索逻辑

- 原四步顺序检索（主队问号 → 重扫 → 2/3/4 问号 → 强制移动）
  已在上游合入时**升级为上游的强制移动等级化模式**（配置
  `ExecuteFixedPatrolScan` 0=关闭 / 1=效率 / 2=保守，旧布尔值
  自动迁移：True→2），行为与原四步检索等效且更完善：
  - 战后先常规重扫；
  - 等级 1：只切队看雷达 + 清问号后补全图扫描，不挪舰队；
  - 等级 2：L1 主队清问号+全图扫 → L2 逐队雷达预检+强制移动+逐队重扫
    （命中即停）→ L3 补自律寻敌；
  - 附带明石保底（多队尝试购买）、摄像机漂移对焦恢复、debug 录屏。
- 本地增强保留：强制移动沿用本地的候选落点+备用点返回目标的两段式
  移动逻辑（合并进 `_move_fleet_to_patrol`）；开箱防溢出
  （`avoid_ap_overflow`，行动力 100-119 直接开工）保持不变。
- 显式调用 `OSMap.clear_question` 清理指定舰队问号，避免多继承
  MRO 误解析到多舰队实现。

### 2. 耄耋相接事件检索逻辑

- 先清主舰队周围问号 → 未处理到事件则重扫 → 重扫无事件再切换
  其他舰队依次清问号。
- 显式调用 `OSMap.clear_question`，避免多继承 MRO 误解析到
  侵蚀1的多舰队实现。
- 合入上游「自律寻敌后扫描雷达问号」时同样显式调用
  `OSMap.clear_question`：上游原实现为裸 `self.clear_question()`，
  在组合类 `OperationSiren` 中会解析到侵蚀1的多舰队版本，
  与上游注释声明的「仅当前舰队雷达，不切换 1~4 队」相反。
  融合后既保留上游的雷达补扫功能，又保持本地的单舰队行为。
- 合入上游「短猫战后 debug 录像」（`_meow_debug_clip`）与
  「战后效率模式强制移动」（`_meow_fixed_patrol_scan`）时，
  上游的 `with self._meow_debug_clip():` 直接包住本地三步检索逻辑，
  两个开关默认关闭（`OpsiMeowfficerFarming.DebugClip` /
  `ExecuteFixedPatrolScan`），开启后与本地检索逻辑并存：
  强制移动在本地检索未解决事件时才执行（其内部按
  `ALREADY_SOLVED_MAP_EVENTS` 自行跳过）。

### 3. 明石商店去重

- 一次战役只处理一次明石商店，强制移动等后续环节不再重复进店。
- 新一轮计划作战开始时重置该标志。

### 4. 地图名称 OCR 稳定性

- 等待 UI 过渡动画完成后再截图，避免读取到不完整的海域名称。

### 5. 智能调度+补黄币跳过重复的初始化自律寻敌

- 智能调度+在黄币补充分支不再补执行初始化阶段跳过的首次自律寻敌，
  避免任务被打断后回到刚清理过的海域时重复全图重扫。
- 黄币补充任务（耄耋相接等）自身会执行战略搜索与事件检索，
  清理职责不缺失。

### 6. 每日任务清理海域三步事件检索

- 每月最后一天清理任务海域时，自律寻敌结束后按三步检索事件，
  任何一步发现并成功解决事件即结束：
  1. 先清理主舰队周围问号；
  2. 未解决则执行一次地图重扫；
  3. 重扫未发现事件则依次切换 2/3/4 舰队清理问号。
- 显式调用 `OSMap.clear_question`，避免组合类 MRO 误解析到
  侵蚀1的多舰队清问号实现。

### 7. 自动搜索清问号显式使用单舰队实现

- `OSMap.run_auto_search()` 内「清近距离问号」显式调用
  `OSMap.clear_question`，避免组合类 MRO 误解析到侵蚀1的多舰队
  清问号实现，修复初始化首次自律寻敌、每日任务等场景的错误舰队切换。

### 8. 耄耋相接明石统计与本月收获表格

- 耄耋相接数据收集表格新增「遇见明石次数 / 遇见明石概率 / 平均体力」
  三列，按侵蚀等级独立统计：运行时在每轮耄耋相接搜索结束后检测
  明石事件并计数，明石商店购买体力时按耄耋相接上下文记录，
  与侵蚀一统计互不干扰（数据从功能上线后开始积累）。
- 「耄耋相接收获」下新增「本月耄耋相接收获」表格（在原有累计数据
  表格上方），按侵蚀等级分列展示当月掉落总数：
  月份、侵蚀等级、金菜（Plate 装备强化板）、彩图纸（GearDesignPlan*T5）、
  金机密（OrdnanceTestingReport*T4）、隐秘（CoordinateObscure）、
  深渊（CoordinateAbyssal）、金猫箱（CatT3）。
- 本月收获表格带「查看历史月份」按钮（弹出月份选择器，数据来源为
  本地掉落明细库中实际存在的月份）与「回到本月」按钮。
- 数据收集表格中原「本月金菜 / 本月深渊 / 本月隐秘」三列随之移除
  （月度收获统一由新表格展示）。
- 从结算截图提取并新增「军用电子元件」第四形态模板
  （`Military_Grade_Electronic_Components_4.png`），修复该形态被识别为
  未识别物品代号 60 的问题。
- 新增「舰炮装备设计图 T4」模板（`GearDesignPlanGunT4.png`，由结算截图
  提取），修复 T4 图标被误匹配为 GunT5 的问题（相似度阈值 0.85 下
  T4/T5 图标可互相误匹配）；对应结算的数据库记录已同步修正。
- 新增「工业级电子元件」第四形态模板
  （`Industrial_Grade_Electronic_Components_4.png`），修复该形态被识别为
  未识别物品代号 62 的问题。
- 新增「B 型火药」第二形态模板（`Poudre_B_Gunpowder_2.png`），修复该
  形态被识别为未识别物品（数字代号 63）的问题。
- 新增「防空装备设计图 T5」第二形态模板
  （`GearDesignPlanAntiAirT5_2.png`，新颜色形态，彩色相似度 0.841 差
  0.85 阈值一点被识别为未知代号 64），提取后识别为防空装备设计图 T5
  （初判误判为 T4，已按实际物品修正模板与数据库）。
- 新增「工业级电子元件」第五形态模板
  （`Industrial_Grade_Electronic_Components_5.png`，同结算与已知工业级
  图标互相关 0.62、其余物品均低于 0.38），修复被识别为未知代号 64。
- 新增「特装原型」模板（`SpecialGearPrototype.png`，由月度结算截图
  提取），修复月度结算中 ×40 的特装原型被识别为未知物品的问题。
- 新增「HBX 混合火药」第二形态模板（`HBX_Blend_Gunpowder_2.png`，
  由耄耋相接结算截图提取），修复该颜色形态与基础模板相似度不足、
  被识别为未知物品的问题。
- 新增「军械测试报告 T3」第四形态模板
  （`OrdnanceTestingReportT3_4.png`，由结算截图提取），修复该形态被
  识别为未知数字代号 67 的问题；对应结算的数据库记录已同步修正。
  注意模板必须取自该结算第 2 排第 6 列（军械测试报告格），若误取
  第 1 排第 3 列（合成弹性材料格）会导致后续 12 个结算的合成弹性
  材料被误判为军械测试报告 T3。
- 新增「军械测试报告 T3」第五形态模板（`OrdnanceTestingReportT3_5.png`，
  淡紫底新形态，由 09-12 02:04 结算提取），修复该形态被识别为未知
  数字代号 70 的问题；对应结算的数据库记录已同步修正。
- 新增「工业级电子元件」第六形态模板
  （`Industrial_Grade_Electronic_Components_6.png`，由 09-09 22:30 结算
  提取），修复该形态（明暗与基础模板不同）被识别为未知数字代号的
  问题；09-09 22:30 与 09-10 15:41 两次结算的数据库记录均已同步修正。
- 仍未识别的历史物品仅剩数字代号 61（72 行）与 62（4 行），无对应
  截图无法辨认，保留原样。
- 仍有未识别物品（新的数字代号，每次运行编号可能不同）待后续截图
  辨认，识别出后按同样流程补模板。
- 奖励页标题模板搜索范围收紧：标题模板只有 35×16，结算页底部新增的
  「本次作战出现紧急委托：…」文字也会匹配成功（相似度甚至高于真
  标题），而 `Button.match()` 会把匹配位置缓存在按钮对象上，代码又
  用该位置推算物品网格原点，于是一次匹配跑偏就让整次结算解析为
  0 项、静默不入库。真标题固定在 y=149、横向随面板宽度浮动，故把
  搜索范围由 `(-80,-20,80,500)` 收紧为 `(-80,-20,80,60)`，并在网格
  为空时补一条 warning 日志；53 次留存结算回归后识别结果与数据库
  零差异，同时恢复了 09-12 01:56 那次被漏记的结算。
- 未识别物品的定位截图统一保存到 `screenshots/unknown_items/`。
  掉落解析时若出现未识别物品（数字代号），脚本自动把该结算截图另存
  到该目录，命名为 `<掉落记录文件名>_未知物品<数字代号>[_帧号].png`，
  图上用红框框出未知物品所在格子、用白字标注其代号（多张截图时
  帧号从 0 开始）。人工辨认后重命名为 `<时间戳>_<物品名>.png`，
  多形态对照图命名 `<物品名>_形态对照.png`。该目录随 `screenshots/`
  一起被 git 忽略，仅作本地核对用。

### 9. 大世界掉落截图仅保存耄耋相接任务

- `DropRecord.OpsiRecord` 开启保存时，仅耄耋相接任务的结算截图
  落盘到本地（侵蚀1练级、每日、隐秘、深渊等任务去掉保存、保留
  上传等行为），避免高频任务截图占用大量磁盘。
- 落盘时仅保留结算奖励页那一帧：一次掉落记录原本由两张截图拼成
  （`map_event.py` 在检测到结算页时截的结算页 + `os/map.py` 在自动
  搜索结束后截的大世界区域页），区域页只是为了给解析提供区域名与
  危险等级，解析在内存中完成、不依赖落盘文件；因此保存前按
  `is_opsi_reward()` 过滤掉区域页，截图打开后就是单纯的结算页
  （多张结算页会全部保留，识别不到结算页时回退保存完整截图）。
  分类与筛选期间会临时改写 `AUTO_SEARCH_REWARD` 的匹配位置缓存，
  用完立即还原，避免影响随后的解析。
  代价：落盘文件不再包含区域页，无法只凭截图重建区域信息（产品中
  没有「重新解析历史截图」功能，仅影响人工回溯）。

### 10. 侵蚀一练级行动力开箱防溢出

- 侵蚀一练级开工前要求行动力 ≥120（预算式检查），但当前行动力在
  **100-119 时直接开工、完全不开启行动力箱**（不再等待自然恢复，
  也不会开 100 箱造成溢出）；低于 100 时按 20→50→100 顺序开箱
  补齐，开箱后达到或超过 200 满值的箱子跳过不开启。
  其余任务的开箱行为不变。

### 11. 图纸/报告等级改按图标底色判定

- 白纸类物品（装备设计图 `GearDesignPlan*`、军械测试报告
  `OrdnanceTestingReport*`）的稀有度只体现在图标底色上：紫底 T3、
  金底 T4、彩底 T5，中间的白纸图案完全相同。
- 底纸带缩放动画，同一物品在不同结算里的纸张大小不同，模板相似度
  因此相差 0.2 以上：实测金底的舰炮图纸与彩虹底 T5 模板相似度 0.92、
  与本级金底 T4 模板仅 0.79，于是被记成彩图纸、虚增彩图纸数量
  （09-12 记下的三条「彩图纸」里有两条其实是金图纸）。
- 现在先按底色定等级，只在同等级模板中匹配；同级候选之间只比图案，
  底纸缩放会拉低相似度，故阈值由 0.85 放宽到 0.6（同级已排除跨级
  误匹配，实测正确模板 0.74~0.89、其它种类 ≤0.64）。
- 底色无法判断、或该等级尚无模板（新形态）时不做限制，仍按原阈值
  识别，避免把未知物品直接判成错误等级。
- 附带修好了 09-12 06:29 那个未识别物品：金底报告按底色限定后匹配到
  军械测试报告 T4（相似度 0.73），无需新增模板。
- 该逻辑以 `AutoSearchItemGrid.match_candidates` 钩子实现，基类
  `ItemGrid` 只新增了一个默认不生效的候选过滤钩子。

## 六、OCR 与设备选择

### 1. 独显优先，避免绑定虚拟显示器适配器

- 现象：OCR 偶发崩溃，日志为 `ONNXRuntimeError` +
  `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xc9`，
  任务被判为未处理异常后中断，并触发 Restart 重启游戏（每次约 1~2 分钟）。
  2026-07-13 曾出现 378 次同类错误。
- 根因：机器装有多个虚拟显示器适配器（GameViewer 远程控制、
  Virtual Display Driver、MuMu 虚拟屏），ONNX Runtime 会为每个适配器
  枚举出一个 DirectML 设备，且它们都镜像真实独显的名称与显存
  （`NVIDIA GeForce RTX 4070 SUPER` / 11999 MB）。上游
  `_is_discrete_gpu()` 对缺失 `Discrete` 元数据的设备退化为
  「名字不像核显 + 显存够大即放行」，于是虚拟适配器也进入候选；
  候选顺序按枚举顺序，结果 OCR 绑到了会被热插拔的虚拟适配器上，
  显示配置变化后会话失效，DirectML 报本地化（GBK）错误消息，
  onnxruntime 的 Python 绑定按 UTF-8 解码失败，最终以
  `UnicodeDecodeError` 掩盖真实错误并中断任务。
- 修改：`module/ocr/windows_ml.py` 新增 `_device_priority()`，
  `_iter_preferred_devices()` 在同一 EP 候选内按
  「`Discrete=1` 优先 → `DxgiHighPerformanceIndex` 升序」排序，
  并逐条打印候选设备（含 adapter 编号与 Discrete 标记）便于排查。
  排序只调整尝试顺序，虚拟适配器仍保留为后备，不影响无独显标记的机器。
- 验证：本机候选顺序由 adapter 2（虚拟）改为 adapter 0
  （真实独显，`Discrete=1`）；真实创建 DirectML 会话并推理成功。
  新增 `tests/test_ocr_device_priority.py` 覆盖该排序规则。

## 维护约定

- 每次新增定制修改并提交时，同步更新本文件对应章节。
- 若某项定制后来被上游合并或被回退，应从本文件移除并在提交说明中注明。
