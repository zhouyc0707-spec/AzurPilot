# AzurPilot 本地定制化修改清单

> 本文件记录本仓库（`zhouyc0707-spec/AzurPilot`）相对上游 `wess09/AzurPilot`
> master 分支的本地定制修改，共 82 个文件有差异。
>
> 对比方式：`git diff wess09/master master`
>
> 最后更新：2026-09-20

## 速查

| 章节 | 条目 | 主题 |
|---|---|---|
| 一、WebUI | 1 | 概览页布局与区域分离 |
| | 2-3 | 资源仪表盘、体力变化图表 |
| | 4-8 | 大世界数据收集（三行合表）、委托收益、每日经验检测、趋势图隐藏、异步渲染 |
| | 9-11 | 统计页视觉统一、窗口缩放自适应、移除导出按钮 |
| | 12-13 | 日志区切换、图表主题与涨跌着色 |
| | 14 | 大世界掉落截图与识别 |
| | 15 | 委托收益「正在进行」区块 |
| | 16 | CL1 只读缓存（渲染性能） |
| | 17 | 多余代码编辑器（切页卡顿） |
| | 18 | 设备ID 指纹采集移出首屏 |
| | 19 | worker 启动后台预热（首次进页冷启动） |
| | 20 | 关闭日志后体力曲线变粗（隐藏期间重绘） |
| 二、岛屿 | 2 项 | 餐品原料不足、排产岗位分配 |
| 三、调度 | 2 项 | 岛屿任务半点对齐、停止按钮响应延迟 |
| 四、战役 | 4 项 | 旗舰复用、活动PT误读、委托掉落开关、数量OCR |
| 五、大世界 | 9 项 | 事件检索、明石去重、统计与模板、月度收获合并、开箱防溢出、图纸等级、大舰队弹窗 |
| 六、OCR | 1 项 | 独显优先 |
| 七、测试 | 1 项 | PyWebIO 输出打桩护栏 |


## 一、WebUI 界面定制

### 1. 概览页布局与区域分离

- 调度器列与右侧统计面板之间增加 `1rem` 列间距。
- 原右侧日志区改为统计图表面板；日志改为可切换的独立区域（见「概览页下方区域切换为日志」）。
- 统计内容拆成两个区域：上方资源仪表盘，下方图表区为独立圆角卡片并自行
  滚动，滚动只发生在图表区（`#pywebio-scope-stat_panels` 用
  `grid-template-rows: auto minmax(0,1fr)`）；窄屏 `max-width:768px` 时
  仪表盘改为 `position:sticky` 吸顶。总览页与独立统计页共用同一套面板。

### 2. 资源仪表盘

- 从日志页移至统计页顶部，横向排列并增加边框。
- 默认显示 8 项：石油、物资、钻石、活动PT、魔方、核心数据、勋章、功勋。
  行动力/黄币/紫币/舰队币不显示，避免与下方体力图表重复。
- 配置来源为 `module/config/argument/dashboard.yaml`（登记 13 项），
  显示清单由 `module/webui/app_dashboard.py` 的 `_update_dashboard`
  `groups_to_display` 参数指定。

### 3. 体力变化图表

- 序列概览行（行动力/黄币/紫币/海里数/资产）字号 12 → 14px，改用
  flex + `gap:2px 20px` 自动换行，不再使用固定列宽。
- 右侧刻度预留宽度 110 → 72px，消除图表右侧空白。
- `+` / `-` 缩放按钮已从面板模板移除（`ap_chart.js` 中读取
  `_zoom_in` / `_zoom_out` 的监听因取不到元素自动跳过）。
- 曲线配色跟随 WebUI 主题，主曲线按涨跌分段着色（见「图表主题适配与涨跌着色」）。
- 时间范围按钮「近24小时 / 近七天 / 本月」放在图例「─体力」左侧，默认
  近七天（整月点数太多、细节被压扁）。近24小时是滚动窗口（`now - 24h`），
  近七天是含今天在内的滚动 7 天，本月取当月全部；辅助序列用同一组时间戳
  过滤后按图表点对齐。
  用独立 i18n 键 `ApPeriodLast24h` / `ApPeriodWeek` / `ApPeriodMonth`，
  不借用委托收益的「本周」（那是自然周口径）。
- 标题与图例留白收紧（标题上边距 16→10px、标题下边距与图例下边距 8→6px）。
- **右侧 y 轴刻度修好**：黄币 / 紫币 / 海里数 / 资产勾选后右侧都没有数字。
  根因不是「漏了某几个序列」，而是**轴配置算一次、绘制在另一条路径**：
  `pad.r` 与 `EXTRA_SERIES_CONFIGS` 原先只在 `initChart` 里算，而图例切换走的是
  `renderDetailChart`（缩放/明细视图），它直接复用这两个值 —— 于是切换后右侧
  留白仍是默认视图的 20px、轴配置仍是空数组，刻度一个都画不出来。
  实测（无头浏览器 + 拦截 `fillText` + 插桩）：`addCfg` 收到的 `has` 全是
  `False`（算配置时 `seriesVisible` 还是默认的「仅体力」），而 `drawAssetTicks`
  运行时 `seriesVisible` 已经变了、`cfgs` 却仍是 0。
  改为把「按当前可见序列重算右侧留白 + 轴配置」抽成 `updateExtraAxes()`，
  在两条绘制路径的绘制前都调用。
  顺带：右侧留白改为按实际刻度行数计算（1 行 56px / 2 行 67px，原先固定 72px
  偏宽，且不再出现只有一行刻度也留 72px 的空带）；每格最多叠 2 行刻度
  （`COIN_TICK_STACK_GAP` 11px 正好容纳 10px 字号，再多会互相压住）；
  刻度数字加千位分隔符（右轴常在十万量级，不分隔难以读数）。
  注意黄币 / 海里数 / 资产**共用同一条轴**，因此只看其中一个时右侧数字范围会
  同时容纳另外两者 —— 这是共用轴的正常表现。
- **标题旁加刷新图标按钮**：点击只重绘图表本身（`_render_ap_chart`）。
  面板 HTML 由 `run_js` 注入，直接在里面写 `#pywebio-scope-xxx` 对不上 PyWebIO
  自己建的 div，因此沿用时间范围按钮的做法：Python 先 `put_scope` 建作用域并
  `put_button` 渲染按钮，再由面板 JS 把节点**就地搬进**标题行右侧的 slot
  （节点不重建，点击回调仍有效）。注意输出顺序：面板 HTML 必须最先。
  样式与其它板块的图标按钮共用一条规则，见「统计页视觉统一」。

### 4. 雪风大人的大世界数据收集（原「侵蚀一数据收集」）

一张表按侵蚀等级分三行，**侵蚀1 与耄耋相接的数据合并显示**，原先单独那张
「耄耋相接数据收集」表已删除。

- 标题由「雪风大人的侵蚀一数据收集」改为「雪风大人的大世界数据收集」。
- 行序为 **侵蚀等级 1 → 5 → 3**：等级 1 取侵蚀1 的数据，3 / 5 取耄耋相接
  （`get_meow_stats(hazard_level=...)`）的数据；表头最前面是「月份 / 侵蚀等级」。
- 列（12 列）：月份、侵蚀等级、战斗场次、出击轮次、出击消耗、遇见明石次数、
  遇见明石概率、平均体力、吊机次数、吊机概率、平均战斗时间、平均一轮时长。
- **出击消耗三行都算**：每轮行动力消耗 × 出击轮次，侵蚀1 每轮 5（1 轮 = 2 场
  × 5）、侵蚀3 每轮 15、侵蚀5 每轮 30。共用表 `AP_COST_PER_ROUND`。
- 表格上方汇总行**四条并排**：当月侵蚀一购买体力 / 侵蚀一出击消耗 /
  侵蚀一净赚体力 / 侵蚀一循环效率。三个派生量都不进表格 —— 它们都是侵蚀1
  独有的口径（侵蚀3/5 不按每轮行动力核算），放表里对 3/5 行只能留空：
  - **净赚体力** = 当月侵蚀一购买体力 − 出击消耗（曾在 `d204452a1` 取消，
    后又恢复为表格列，最终改由汇总行显示）；
  - **循环效率** = 净赚体力 / 出击消耗；
  - 只有第一条带「当月」，其余三条省略以免连写四个「当月」，也少占约 100px
    宽度（**汇总行要能放下一行**，见下）。
- **汇总行用 `put_html` + flex 而不是 `put_row`**：`put_row` 生成的是 grid
  （`grid-auto-flow: column` + `grid-template-columns`），窗口一窄就把几条挤成
  多行、间距也依赖 `None` 占位项。改成 `flex-wrap: wrap` + `column-gap: 24px`
  后换行行为可预期。实测：1500px 窗口占 1 行，1200px 占 2 行，1000px 以下
  占 3 行。文案长度控制在「四项合计 ≤ 容器宽 − 间距」，否则会退化成两行。
- **正负着色（正数红、负数深绿）只作用于数值**，标签保持默认色 —— 整条染色会
  让文字标签也变红/绿，读起来像标题出错。色值刻意不跟随主题主色（红涨绿跌是
  语义色），深浅主题各一档（`#d32f2f`/`#00897b` 与 `#ef5350`/`#26a69a`），
  与体力图表的涨跌色同一组取值。
  实现上是**内联注入 `<style>`**（色值定义在 `OpsiStatisticsMixin` 常量上），
  **不走主题 CSS 变量**：变量曾散在四个主题文件里，漏掉任何一个（默认的
  `light-alas.css` 就漏过一次）都会让 `color` 解析失败、静默回退成继承色。
  标签与数值从 i18n 文案按最后一个冒号拆开，外层套 `white-space: nowrap`
  防止窄屏把数值甩到下一行。
- **汇总行到标题与表格的间距**：`entry-alas.css` 的
  `#pywebio-scope-opsi_stats .stat-summary` 给出「标题 → 汇总文字 → 表格」各
  8px。上边距取 0 是因为标题自身已有 8px 下边距，两处都写会叠加成 16px。
  规则用 scope 限定，只作用于本板块 —— 「本月耄耋相接收获」与「每日经验检测」
  的间距保持原样（它们的汇总行虽然也带 `stat-summary` 类，但不在这个 scope 下）。
- **循环效率与经验效率从表格移除**：两者都是侵蚀1 独有的口径（经验统计只跑
  侵蚀1），放表里对 3/5 行只能留空。循环效率改由汇总行显示；经验效率去掉
  （`Gui.Stat.ExpEfficiencyHeader` 键随之删除，`ExpEfficiency` 仍被「每日经验
  检测」使用）。
- 「战斗轮次」列改名「出击轮次」（`Gui.Stat.BattleRounds`，五种语言），与世界
  地图口径统一。注意该键也被导出表（`app_stat_opsi_export.py`）使用，导出表
  列名会同步变化。
- **侵蚀3 / 侵蚀5 的「出击轮次」按整数展示（两张表都改了）**：耄耋相接的
  `effective_rounds` 是 float（由战斗场次之类的比值算出，生产库里实测侵蚀5 是
  `711.33`），而侵蚀1 那行是 `(battles + 1) // 2` 的整数 —— 同一列两种形态看起来
  不一致。展示时统一四舍五入：
  - 「雪风大人的大世界数据收集」三行表 → `app_stat_opsi._format_rounds()`，
    返回值与侵蚀1 行同为 `int`，取不到值时给 `DASH`；
  - 「本月耄耋相接收获」 → `app_stat_opsi_export._render_monthly_meow_loot()`。
    这里原先写的是 `round(x, 1)` 后**只在接近整数时才转 int**，于是 `711.33`
    会显示成 `711.3`；而同一张表右侧的累计轮数（`_meow_extra_columns`）早就是
    `int(round(...))`，同一行两种精度。现在统一成 `int(round(...))`。
  - **只改展示、不改口径**：数据收集表的「出击消耗 = 每轮消耗 × 轮次」仍用未取整的
    原值算（360.8 × 15 = 5412，而不是 361 × 15 = 5415）。
  - 回归测试 `tests/test_webui_opsi_rounds_display.py` 同时锁住两张表。
- 新增 i18n 键 `MonthlySortieCost`（侵蚀一出击消耗）、`MonthlyNetAP`
  （侵蚀一净赚体力）、`MonthlyLoopEfficiency`（侵蚀一循环效率）。
- 历史保留项：「今日战斗 / 今日经验 / 今日运行时间」三列早已删除；「吊机次数 /
  吊机概率」与「遇见明石次数 / 遇见明石概率」的列序按现在的表头排列。

### 5. 委托收益统计

- 「委托次数 / 平均/次」原先是独立表格，现并入获得数量下方的五张物品卡片
  （`{count}次 · 平均/次 {avg}`，五种语言），删除原表格。
- 刷新按钮改为**标题「委托收益统计」右侧的圆环箭头图标按钮**（SVG data URI
  作按钮背景图，深浅两套按主题切换）；「每日经验检测」的刷新按钮同样处理。
  图标实现抽到共用模块 `module/webui/stat_icon.py`，标题行用
  `.stat-title-row` 排布（标题过长省略号截断，图标不参与收缩）。
- 最近委托记录加载最近 50 条、单页 10 条，支持上一页/下一页、1~5 页码跳转
  与页码显示；去掉分页控件上重复的「第 x / y 页」文字（按钮组已高亮当前页）。
- 截图查看：委托结算时把「获取物品」弹窗截图落盘到
  `log/commission_rewards/<实例>/<YYYY-MM>/`，路径写入数据库记录；统计页每条
  有截图的记录显示「查看截图」按钮，在新标签页打开。**一次委托收获（一个
  「获得道具」弹窗）= 一条记录 = 一张截图**：同一次领取里拿到几次就写几条记录，
  绝不把多次收获并进同一条记录。`_record_commission_income` 因此逐张识别、逐张
  落盘、逐条入库（跨截图汇总只用于推送通知），`add_commission_income` 在存储
  边界兜底丢弃多余的截图。只保留最近 50 张。新增静态路由
  `/static/commission_rewards`（只暴露截图，不暴露日志；旧界面挂在
  `module/webui/app.py`，新 API 侧挂在 `module/api/app.py`，两边路径结构一致，
  便于在新前端里还原旧统计页时复用同一批链接）。旧记录无截图字段时
  不显示按钮，自然降级。
- 「查看截图」按钮的样式与同页按钮对齐（`assets/gui/css/entry-alas.css` 的
  `.alas-shot-link`，30px 高 / 10px 圆角 / 12px 字号 / 600 字重，配色走
  `--alas-entry-*` 变量，含悬停/按下/键盘聚焦反馈）；文案改用 i18n 键
  `Gui.Stat.ViewScreenshot`（原先硬编码中文，五种语言）。

### 6. 每日经验检测统计行合并

- 不再显示「平均战斗时间」「平均一轮侵蚀1时长」。
- 汇总收成**一行**：`上次检查时间 / 今日经验 / 经验效率 / 今日运行`。原先
  「上次检查时间」与今日统计各占一行，行数偏多，现合并进同一个 `put_row`；
  列间距由 `entry-alas.css` 的 `--ship-exp-summary--` 规则给出（`put_row`
  生成的是 grid，默认没有列间距，几段文字会挤在一起）。
- **各项间距过大的真正原因是 `put_row` 缺省列宽**：不传 `size` 时 PyWebIO 会把
  每一项都设成 `1fr`（等分整行）—— 实测 `grid-template-columns` 为
  `278px 10px 278px …`，而文字只占 46~150px，每项多出 130~230px 空白。
  只调 `column-gap` 看不出效果就是这个原因（先后调过 1.5rem / .75rem）。
  现改为 `size="auto auto … 1fr"`（按内容取宽 + 末尾吸收剩余空间），实测列宽
  变成 `213px 105px 129px 108px 587px`，各项自然靠拢；`column-gap` 保留 .75rem。
- **「今日战斗」不再显示**：下方表格里已有战斗场次，重复了。
  `TodayBattles` / `TodayBattleUnit` 两个 i18n 键随之删除。
- 表格「平均一轮耄耋相接时长」列标题精简为「平均一轮时长」（五种语言同步）。

### 7. 全资源变化趋势图表

- 整体隐藏，不再渲染，也不注册周期刷新。

### 8. 总览页统计面板异步渲染

- 进入总览页或来回切换时，统计视图（资源概览、体力图表、大世界统计、
  舰船经验、委托收益）不再同步渲染，改为只挂载空容器、由后台任务线程立即
  填充，消除页面切换卡顿。
- **后续实测**：切页面路径本身很轻（`_mount_stat_panels` 实测 0.1 ms），
  主要耗时在后台渲染任务里 —— 见「16. CL1 只读缓存」。

### 9. 统计页视觉统一

- **标题字号**：各板块标题统一 `1rem`；行动力/黄币/紫币/海里数/资产五行统计
  文字 14px；委托收益标题字重统一 `600`。
- **板块间距**：原先各写各的（12px / 20px / 内联 20px 混用），统一由
  `.stat-section` 收口为**上 24px / 下 8px**，样式随面板注入一次；标题构造
  改用 `build_stat_section_title()`，调用处不再各传 margin。以后调整全局间距
  只需改 `module/webui/app_helpers.py` 的 `STAT_SECTION_STYLE` 一处。
- **按钮样式**：统计页所有按钮统一为「中性描边 + 选中主色实心」，分段按钮
  （时间范围、分页、月份选择）合并为一组共享外框。样式放在
  `assets/gui/css/entry-alas.css` 的「统计页统一按钮样式」区块（不再由 Python
  运行时注入），作用域限定在统计各区域。
- **标题旁的刷新图标按钮**：委托收益、每日经验检测、体力变化图表、
  本月耄耋相接收获、雪风大人的大世界数据收集**五个板块**统一为
  「标题 + 图标按钮同排」，个个都只刷新自己所属区域。
  要点与踩坑：
  - 图标按钮是空 label 的 `put_button`，图标本体由 Python 以 data URI 背景图
    注入（深浅两套按主题切换），外框由 CSS 收口：30px 见方、10px 圆角。
  - 五个作用域原先**四处选择器列表各写一份**，新增按钮时容易漏 —— 已合并成
    一个 `:is(...)`，加按钮只改这一处。
  - **图表那个按钮的作用域名带运行时生成的图表 id**（`ap_cv_<id>_refresh`，
    见 `chart_id = f"ap_cv_{id(self)}"`），写死成 `#pywebio-scope-ap_chart_refresh`
    永远匹配不上 → 它只会命中通用的「统计页按钮」规则，被套上
    `width:auto` + `padding:0 14px`，比其他四个宽一圈。现改用属性选择器
    `[id^="pywebio-scope-ap_cv_"][id$="_refresh"]` 匹配。
  - 三个 `build_title_icon_row` 生成的标题行是普通 `div`，必须有
    `display:flex`，否则标题与图标各占一行、标题也拿不到 `.stat-title-text`
    的 1rem/600 字号（曾漏掉 `#pywebio-scope-opsi_stats`）。
  - 耄耋收获的刷新按钮作用域在 `put_row` 的标题行里建：`put_scope` 遇到同名元素
    仍在 DOM 时会插入灰条报错（`duplicated_scope_name`），所以不能放在会因切月份
    重复调用的 `_render_monthly_meow_loot` 里，那里只渲染按钮本体。
- 修掉两处布局问题：分段容器按基线对齐导致按钮组整体下沉（改
  `align-items:center` + `vertical-align:middle`，清掉主题 `.btn-off` 残留的
  2px 外边距）；按钮被 `alas-mobile.css` 的 `width:100%!important` 拉成整行宽
  （统一样式显式钉住按内容取宽，并固定高度/内边距/行高抵消移动端覆盖）。

### 10. 窗口缩放自适应

- 体力图表与资源趋势图表的 Canvas 原先只在加载时读一次尺寸，窗口缩放后不跟随。
- 修复：resize 时防抖 150ms 重绘；画布宽度恢复 CSS 百分比宽度后再读取布局
  宽度（原实现把宽度钉死成像素值，重绘也取不到新宽度）；重绘前先移除旧监听器
  避免重复绑定；缩放/平移状态提升到闭包层，缩放后调整窗口大小仍保持当前视图。

### 11. 移除耄耋相接收获下方的刷新与导出按钮

- 「刷新」与「导出并保存到桌面」（导出 CSV 到桌面）两个按钮已无保留价值，
  按钮与回调一并删除；失去调用方的 `_export_opsi_csv`（约 120 行）与相关导入
  同步清理。
- `CsvFilenameTemplate` / `SavedToDesktop` 等翻译键保留未删（无入口但不影响
  使用，便于日后把导出改到别处）。

### 12. 概览页下方区域切换为日志

- 点左侧「日志 / 打开」按钮，把概览页下方那块滚动卡片区就地换成日志；按钮
  文案变成「关闭日志」，再点一次切回统计图表。模式记在 localStorage，刷新或
  切换页面后保持；独立统计页始终显示图表区。
- 切换只改显示状态（`#pywebio-scope-stat_panels_log` 与
  `#pywebio-scope-stat_panels_charts` 互斥），不重建 scope、不重绘图表。
- 日志区沿用原日志页组件（RichLog 栏、自动滚动、截图预览），自动滚动改由前端
  `MutationObserver` 实现：「自动滚动」按钮负责注册/注销观察器，用户往上翻时
  暂停跟随、回到底部附近恢复。
- 新增 i18n 键 `Gui.Button.CloseLog`（五种语言），复用左侧原有的「打开」按钮。
- 修掉三个上游原有实现里的隐患（都会让日志区永远空白）：
  1. 日志容器若用 `put_html` 生成的 div 当 scope，`put_buttons`/`put_html` 的
     输出会落到空处 —— PyWebIO 只认 `put_scope` 创建的作用域。
  2. `RichLog.enable_auto_scroll` 用 `str.format` 拼 JS 时，JS 对象字面量的
     花括号与 format 占位符冲突，抛 `ValueError: unexpected '{' in field name`，
     连带整个切换回调失败；改为字符串拼接。
  3. 日志刷新任务交给任务调度器时只会被执行一次（生成器被 `get_generator`
     包装后不再逐帧调用）；改为普通函数（零参 lambda）与其它统计刷新任务一致。
- 日志数据源改为**直接跟随日志文件**：原先读进程管理器的 `renderables` 缓冲，
  但日志实际由各 worker 进程写入 `log/<日期>_<实例名>.txt`（`set_file_logger`），
  WebUI 进程里的 `renderables` 实测始终为空。现在按字节偏移增量读取、单帧最多
  64KB、只消费到最后一个换行；首次跟随回读文件末尾 32KB；文件被轮转/清空时加
  分隔行后从头跟随，内容不再被清掉。日志正文不再经过 `RichLog.console` 渲染
  （该 console 是实例级共享状态，与原日志页刷新任务同时使用时 `export_html` 的
  记录缓冲会互相覆盖，实测渲染出空串），改为转义成
  `<pre class="alas-log-line">` 追加。
- 日志的追加/清空用 `put_html` / `clear`：任务处理线程里 `run_js` 拿不到会话、
  命令会被静默丢弃。
- 跟随任务与面板挂载解耦：概览页重挂载会重建 `RichLog` 实例，且
  `init_menu` → `remove_pending_task()` 会把待删任务一并移除（日志跟随任务在
  其中）。现每帧回到任务处理器的真实列表里核对、缺失即补注册，并在面板
  （重）挂载时重新渲染面板、丢弃跟随位置重新回读文件末尾。

### 15. 委托收益「正在进行」区块

在「最近委托记录」上方显示当前正在进行的委托，每个委托一个圆角卡片横向排成
一行，卡片内是委托名 + `预计 HH:MM 完成`；不受「今日 / 本周 / 本月」影响
（它显示的是当前状态，与所选时间范围无关）。

**钻石委托**（要员 / 度假 / 巡视护卫，即 BIW/NYB 那六个）用**金色边框**标出：
边框加粗到 2px 并换成金色 `#d4a017`，底色给一层很淡的金，在整排白卡片里一眼
可辨。金色刻意不跟随主题变量 —— 它是「稀有」的语义色，四个主题下都要是金色
才认得出来。判定用 `Commission.genre == 'urgent_gem'`：各语言的名称字典
（`project_data.dictionary_cn/en/jp/tw`）本身就按语言把「要员 / 度假 / 巡视」
等关键词映射到这个 genre，因此比硬编码六个委托名可靠 —— 五语言通用，将来新增
同类变体也不用改代码。`rare` 字段写进状态文件，旧文件缺这个字段时按非稀有处理
（下次委托扫描会补上）。

- **数据通道**：委托的运行状态只存在于 ALAS worker 进程的内存里
  （`Commission.finish_time` 由 `create_time + duration` 得出），WebUI 是另一个
  进程，原先拿不到。新增 `module/commission/running_state.py` 用状态文件
  `log/commission_running_<实例>.json` 把两边接起来：worker 在
  `commission.py` 的 `run()` 里扫描并启动委托之后写入，WebUI 读它渲染。
  放 `log/` 而不是 `config/`：它是纯运行时产物（每次扫描覆盖重建，丢了下次
  扫描就补上），而 `config/` 语义上是用户配置；日志与委托截图也都在 `log/`。
- **完成时间用「扫描时刻 + 剩余时间」**，不是 `Commission.finish_time`
  —— 后者是 `create_time + 总时长`，对扫描时就已在跑的委托会算早；OCR 读到的
  duration 对运行中的委托正是剩余时间，因此 `now + duration` 在任何情况下都对。
- **存 epoch 秒而不是格式化字符串**：显示时按本地时区渲染，避免把 worker 的
  时区写死进文件。
- **沿用上一轮的完成时间**（差异 ≤60 秒时）：否则每次扫描差几十秒，页面上这个
  时刻会一直小幅跳动，看着像算错了。
- 读侧两个兜底：**过期条目自动过滤**（`finish <= now` 丢弃，因为 worker 可能已
  停止更新而时间在走）、**损坏文件不崩**（JSON 解析失败降级为「尚未获取」，
  不让整个委托收益板块渲染不出来）。写侧先写 `.tmp` 再 `os.replace` 原子替换，
  避免读到写了一半的内容。
- 区分两种空态：状态文件还没生成 → 「尚未获取到委托状态」（还没看）；文件在但
  确实没有运行中的委托 → 「当前无运行中委托」（看过了，确实没有）。
- 新增 i18n 键 `RunningCommissionTitle` / `RunningCommissionFinish` /
  `RunningCommissionNone` / `RunningCommissionUnknown`（五种语言）。
- 同批调整：每日经验检测汇总行的列间距由 `1.5rem` 收到 `.75rem`（1.5rem 时几段
  之间空得太大，与标题行不成比例）。

### 16. CL1 只读缓存（渲染性能）

实测下来「切页面」本身很轻（`_mount_stat_panels` 0.1 ms），卡顿来自后台渲染任务
重复解析同一份月度 blob：

- `cl1_data.db` 的月度数据以**单块 JSON** 存储，2026-08 有 **4.01 MB**、
  `ap_snapshots` 2.2 万条；每次 `get_stats()` 都要整块 `json.loads`，单次
  **17~34 ms**。
- 一次渲染会经由多条路径重复读取同一个月：`get_meow_stats` 每个侵蚀等级各一次、
  `_build_cl1_summary`、`_load_meow_cumulative_rows` 等，累计 **150~200 ms**。
- 实测：`_render_opsi_stats` **192.5 → 78.4 ms**（2.5×），
  `_render_meowofficer_farming` **62.6 → 38.1 ms**（1.6×）。

做法（`module/statistics/cl1_database.py`）：

- 新增**只读缓存**，默认**关闭**（其它调用方行为不变），由只读渲染路径用
  `with cl1_db.read_cache():` 开启，退出即关闭并清空 —— 用 with 保证异常路径
  也会关掉（忘了关会让写路径读到旧数据）。
- 失效有三重保障：① 唯一写入口 `save_stats` 清缓存；② 加密行迁移的批量
  `UPDATE` 后也清；③ 缓存条目记录**库文件的（mtime, size）**，两者任一变化即
  视为已更新 —— worker 与 WebUI 是两个进程，进程内的失效看不到对方的写入。
- 另加 `READ_CACHE_TTL = 2s` 兜底：同一渲染周期内命中，跨周期必然重新查库。
- **注意**：缓存返回的是同一个 dict 对象，写路径（读-改-写）不要开启；WebUI 侧
  只有 `get_meow_stats` 两处调用、确认只读。
- 语义测试见 `tests/test_cl1_read_cache.py`（默认关闭 / 命中同一对象 / 退出与
  异常都清空 / 写入失效 / 外部写入靠签名识别 / 超 TTL 失效）。

### 17. 纯文本字段不再强制挂代码编辑器（切页卡顿）

「智慧港区设置」切页比其他页慢一截的根因：`module/webui/widgets.py` 的
`put_arg_textarea()` **无条件**给每个多行文本参数加了
`code={"lineWrapping": …, "mode": mode}`，而 PyWebIO 只要 `code` 非空就会实例化
一个 **CodeMirror**。`argument.yaml` 里真正声明了 `mode: yaml` 的只有 4 个参数
（`Error.OnePushConfig`、`GemsFarming.EquipmentCode`、`OpsiGeneral.OpsiOnePushConfig`、
`Submarine.AdvancedConfig`），**另外 64 个**（路径、SSH 公钥、启动/停止命令、
API Key、各种 Filter 列表）都被顺带挂上了编辑器。

每个 CodeMirror 实例要构建上百个节点并反复测量样式，初始化开销数十毫秒。
生产实例（22267）上的实测：

| 页面 | 点击→控件出现 | DOM 节点 | textarea | CodeMirror | bootstrap-select |
|---|---|---|---|---|---|
| 智慧港区设置 | **592 / 612 ms** | 2286 | 16 | **8** | 20 |
| 委托 | 95 ms | 655 | 4 | 2 | 1 |
| 科研 | 76 ms | 696 | 2 | 1 | 4 |

`PerformanceObserver(longtask)` 量到切到智慧港区设置时产生一个 **553 ms 长任务**
（委托只有 62~79 ms）；CDP CPU 采样 profiler 归因显示 **CodeMirror 内部函数
（`Hr` / `Dn` / `di` / `an` / `fn`）合计约 300 ms**，占长任务一半以上；
`bootstrap-select` 的 `liHeight` 只有 7~13 ms，不是主因。

改法：只有声明了 `mode` 的参数才保留 `code=`，其余显式 `code=None`。智慧港区
设置页的 8 个 CodeMirror 立即降到 1 个（只剩真正需要 YAML 高亮的推送配置）。
配置搜索的聚焦脚本（`module/webui/config_search.py`）本就带
`input/textarea/select` 兜底分支，原生 textarea 仍能正常聚焦高亮。

回归测试见 `tests/test_webui_performance.py`
（`test_plain_textarea_does_not_instantiate_codemirror`）。

**注意**：`module/webui/widgets.py` 由 WebUI 进程在启动时导入，改完要**重启
WebUI**（或等下一次启动）才生效。

### 18. 设备ID 指纹采集移出首屏（启动慢）

重启后的「加载时间长」有两段，实测拆开（时间戳取自 `log/*_gui.txt`）：

| 阶段 | 耗时 | 说明 |
|---|---|---|
| supervisor 启动到拉起子进程 | ~2.0 s | 含回收上次残留 worker |
| 子进程导入 + 监听 | ~1.29 s | `import module.webui.app` 0.93 s 是主体 |
| **首个页面渲染** | **~2.1 s → ~1.25 s** | 其中 860 ms 是设备ID 的 4 次 wmic |
| 会话初始化到就绪 | ~1.36 s | 含网络校时、配置绑定、建统计索引 |

根因在第三段：`get_device_id()` 首次调用会跑 **4 次 `wmic` 子进程**
（baseboard / cpu / bios / diskdrive，每次 118~137 ms，合计 0.5~1.1 s），而
`module/webui/app.py` 的 `_block_restricted_device()` 在**每个会话建立时**都会
调用它 —— 于是这份开销直接压在首个页面渲染上，日志里就是「WebUI 已就绪」
到「设备ID initialized」之间那 860 ms。

改法（`module/base/device_id.py`）：

- 已有 `log/device_id.json` 时**直接沿用登记值**，把指纹采集丢到后台线程
  （`device-id-verify`）核对。
- 后台核对**只认指纹、不擅自换 ID**：一致就只刷新时间戳；不一致才把缓存值记为
  `_old_device_id` 并改用新指纹 —— 设备ID 是 `opsi_items` 等表的归属键
  （查询都带 `device_id = ?`），换掉就读不到历史数据，所以不能为了快而改它。
- 没有缓存文件（全新安装、或只起了 WebUI 没跑过 alas）时仍**同步生成**，否则设备ID
  会是空的。
- 加 `_init_lock` 保证多线程只初始化一次。

实测：`alas_set_group('Alas')` 首次 **896.9 → 299.7 ms**（走同步 wmic 的对照值
896.9 ms）。缓存文件是 gitignore 的 `log/device_id.json`，本次改动后 ID 不变
（`f8eb9de0…`）。

回归测试见 `tests/test_device_id_fast_path.py`（慢指纹下立即返回 / 后台仍能发现
硬件变化并保留旧 ID / 缺文件回退同步 / 文件损坏回退同步）。

**未做（避免过度改动）**：子进程导入那 0.93 s（`module.webui.app` 连锁
`module.logger` → rich、`webui.setting`、`webui.utils` → pywebio），以及
`State.init()`、会话初始化里的网络校时。这些占剩余启动时间的大头，但要么牵动
所有渲染模块的导入，要么需要改架构，收益与风险不成比例。

### 19. worker 启动后台预热（刚重启后第一次进页慢）

症状：**刚重启后第一次**进 `alas` 实例的总览页/配置页慢，之后就正常。原因是
`alas` 实例 worker 是新进程，三件事都压在首次进页的路径上：

- 统计相关模块是惰性导入（`module.statistics.*` 连带 `cv2` / `numpy`），首次渲染
  才现拉；实测 `import module.webui.app` 单独 926 ms。
- **首次 `load_config('alas')` 实测 249 ms，第二次只要 10 ms 量级** —— 这是最大
  的一块，正好落在 `ui_alas` 切换实例的路径上。
- `azurstats_local.db`(38 MB) / `cl1_data.db`(17 MB) 首次开连接的冷页读取。

改法（新增 `module/webui/warmup.py`，在 `app_home.py` 的 `run()` 里
`mount_shell()` 之后调 `start_warmup()`）：外壳画完、开始等浏览器 localStorage
往返的空档里，用一条 daemon 线程把上面几件事先跑一遍。

A/B 实测（同一机器，两段路径合计）：

| | 段一 `load_config` + `read_file` | 段二 `alas_set_group('Alas')` | 合计 |
|---|---|---|---|
| 不预热 | 249.4 ms | 293.8 ms | **543.2 ms** |
| 预热后 | **16.6 ms** | 278.7 ms | **295.3 ms** |

预热自身总成本 260~340 ms，全在后台，不占主线程等待。

三条硬约束（都由 `tests/test_webui_warmup.py` 守着）：

- **只预热、不改写**：统计库一律以 `file:…?mode=ro` 只读打开，绝不实例化
  `Cl1Database`（它的 `__init__` 会建表并可能迁移）—— 测试会断言 URI 含
  `mode=ro`、且预热前后库文件字节与 mtime 都不变。
- **失败静默**：任一步异常只记一条 warning 并继续，绝不影响启动。
- **每进程一次**：锁 + 标记位，预热约 1~2 s，不能每会话重复跑。

### 20. 关闭日志后体力曲线变粗（隐藏期间重绘写死了 fallback 尺寸）

**症状**：长时间开着日志界面后关掉，体力变化图表的曲线变粗，整页刷新才恢复。

**取证过程**：这个症状靠读代码猜不出来 —— 我先后提出并被推翻的三个假设都没命中。
最后是写了一个临时探针（在每次 `stroke` 时记录 `getTransform().a`、`lineWidth`
与实际生效线宽）粘到 DevTools console，在你机器上抓了一份现场数据才定位到。
**该探针已在定位完成后删除**；要复现同类问题，按下面三点重写一个即可：

- 打桩 `CanvasRenderingContext2D.prototype.stroke`，读取 `this.lineWidth` 与
  `this.getTransform().a`（实际生效线宽 = 两者相乘）；
- 每次调用打一个 `__probeLabel("这一步叫什么")` 分段，末尾导出 JSON；
- 主曲线固定 `lineWidth = 1.8`，所以「线宽不是 1.8」或「scale 不是 dpr」都能一眼看出。

数据本身是决定性的，**前面的三个假设全被它否掉了**：

- dpr = **1**（不是高分屏），Edge 缩放也是 1 → 与 DPI 缩放无关；
- 每次描边的 `scale` 稳定是 **1**、`lineWidth` 稳定是 **1.8** → 变换矩阵没问题；
- 三份报告的差异点是**画布尺寸**，不是线宽。

**真正的根因**（来自探针报告）：

| 段 | 描边条数 | `canvas.width` | `clientWidth` |
|---|---|---|---|
| 日志打开前 | 8850 | 731 | 731 |
| **图表隐藏期间**（60 秒周期重绘两次） | **17700** | **800** | **0** |

`_render_ap_chart` 不检查可见性，60 秒周期重绘在图表 `display:none` 时照样跑。
此时 `cv.clientWidth/clientHeight` 都是 0，`initChart()` 落到 fallback 的
**800×360**；但实际显示尺寸是 **731×262**（`height: clamp(260px, 34vh, 360px)`
在 1200 高的屏幕上算出 262px），纵向被压掉 **27%**。`display` 切换**不触发
`window.resize`**，所以关掉日志后没人重新量尺寸，画布一直带着这个错误的内部分辨率
——曲线被纵向压缩，看起来又粗又陡，只有整页刷新（重建画布）才恢复。

**改法**：

- `webapp/ap_chart.js` 暴露 `window._alasApChartRelayout[chartId]`，在画布可见时
  检查「内部分辨率是否与显示尺寸相符」，不符就重绘。
  **判断依据必须是内部分辨率而不是显示尺寸** —— 恢复可见时显示尺寸本来就没变
  （一直是 731×262），变的只是它在隐藏期间被错误重绘过；我第一版按显示尺寸去重，
  结果误判成「没变」直接跳过（反向验证时才暴露）。
  不用 `window.resize` 是因为 `display` 切换不触发它；也不用 rAF 轮询，因为每帧读
  `clientWidth` 会强制重排，长期开着太贵。
- `module/webui/app_overview.py` 的 `_apply_log_mode_display()` 在切完 `display`
  之后调用一次这个注册表（顺序不能反，否则量到的还是旧布局）。
- 图表重建时通过新增的 `cleanupCallbacks` 把注册项删掉，避免旧闭包被调用。

**验证**（最小页面复刻现场条件：显示 731px 宽、262px 高）：

| 步骤 | 修复前 | 修复后 |
|---|---|---|
| 初始（可见） | 717×262 | 717×262 |
| 隐藏期间重绘 | 800×360 | 800×360 |
| **关掉日志后** | **800×360（错的）** | **717×262 ✓** |

反向验证：把通知那一行注释掉，恢复后尺寸就停在 800×360 —— 与你在生产上抓到的
报告完全一致，确认这就是你看到的现象。

**顺带修掉的两处真实缺陷**：

- `initChart()` 用 `ctx.scale(dpr, dpr)` 设置缩放，而 `ctx.scale` 是**乘法叠加**、
  `ctx.setTransform` 是直接赋值。同一目录的 `resource_chart.js` 一直用的是
  `setTransform`，两份脚本不一致本身就是缺陷。已对齐（语义等价，只是不再依赖
  「设置 `canvas.width` 会重置变换矩阵」这一隐含前提）。**注意**：这一条**不是**本次
  症状的原因（探针数据证明 `scale` 一直是 1），属于顺手修正。
- `resizeTimer` 没有纳入 `cleanup`：图表每 60 秒重建一次，重建后旧闭包的防抖定时器
  仍会触发，白跑一次针对已丢弃闭包的 `initChart()`。

**生效方式**：`read_webapp_template()` 是**每次渲染都从磁盘读**，所以点一下图表
刷新按钮（或等 60 秒周期重绘）即可生效，不需要重启 WebUI。

回归测试见 `tests/test_webui_chart_canvas_scale.py`（7 项）：两个脚本都必须用
`setTransform` 且不得出现 `ctx.scale(dpr, dpr)`；cleanup 必须撤销定时器；叠加层
每个绘制入口都要复位变换矩阵；`_alasApChartRelayout` 注册表必须存在、必须按内部
分辨率判断、必须能反注册；日志开关必须先切 `display` 再通知。

### 13. 图表主题适配与涨跌着色

- 图表画布读不到 CSS 变量，配色由 Python 侧按当前 WebUI 主题注入
  （`module/webui/ap_chart_theme.py`）：浅色主题族（default、
  advanced_material）用白底 + 深一档序列色，深色主题族（dark、
  dark_advanced_material）沿用深底配色。
- 主曲线**按涨跌逐段着色：上升红、下降绿**，四个主题一致（此前浅色主题走
  「主题色单线 + 面积渐变」，整条线只有一种颜色，看不出体力在涨还是在掉）；
  点稀疏时补画的数据点也跟随涨跌。
- 浅色主题涨跌色取更深一档（`#d32f2f` / `#00897b`）保证白底对比度；主色
  `ap_line` 与上升色 `inc` 对齐，图例色块与曲线颜色一致；tooltip 的体力数值
  改用常规文字色。

### 14. 大世界掉落截图与识别
- 掉落截图仅保存耄耋相接任务（`module/os/fleet.py` 改用
  `opsi_save_method(task_command, ...)` 判断）。
- 图纸 / 报告等级改按图标底色判定：白纸类物品（`GearDesignPlan*`、
  `OrdnanceTestingReport*`）的稀有度只体现在图标底色（紫底 T3 / 金底 T4 /
  彩底 T5），中间白纸图案完全相同。底纸带缩放动画导致同一物品在不同结算里
  模板相似度相差 0.2 以上，金底舰炮图纸会匹配到彩底 T5 模板而虚增彩图纸。
  现在先按底色定等级、只在同级模板中匹配，同级候选之间只比图案，阈值由 0.85
  放宽到 0.6；底色无法判断或该等级尚无模板时不限制，仍按原阈值识别。
  以 `AutoSearchItemGrid.match_candidates` 钩子实现。

## 二、岛屿计划

- **餐品原料不足时不再反复无效点击**：游戏 UI 拒绝生产时先把该餐品标记为
  「本轮不可用」，同一 run 内后续扫描直接跳过（严格模式也无产出后才标记）。
- **最低保留数量排产不再占满岗位**：每个未达标作物最多分配一个岗位，避免
  香蕉缺 1 个却四个岗位全种香蕉。

## 三、调度与停止

- **岛屿任务对齐到半点**：`module/config/config.py` 的 `task_delay` 对
  `Island*` 任务统一 `_round_up_half_hour()`，推迟到下一个 `XX:00` / `XX:30`，
  降低调度频次。
- **停止按钮状态不再卡顿**：点击停止到按钮变成「启动」原先要等 2 秒左右，
  由两段延迟叠加而成，现均已消除（实测 `stop_by_user` 耗时 1.015s → 0.102s）：
  1. **收尾动作不阻塞刷新**：停止后的收尾动作（返回主页 / 关闭游戏 / 关闭模拟器）
     改由独立进程 + 后台线程回收，不再卡住「停止 → 开始」的按钮状态刷新。
  2. **停止流程不再空等日志队列线程**：该线程原先靠 `alive`（同样需要实例生命
     周期锁）判断自己是否该结束，与持锁等它退出的停止流程互相僵持满一个 join
     超时（1 秒），这期间按钮状态读不到 `alive`。现改为线程私有的退出信号（置位
     后不再触碰 `alive`）、队列轮询间隔 1s → 0.25s，日志线程读取存活状态改为限时
     抢锁（0.05s），worker 未确认停止时不再无意义 join。
  3. **点击后立即重绘按钮**：按钮文案原先只由 1 秒间隔的轮询任务刷新，点击本身
     不唤醒它，最多再等 1 秒。现在启停回调结束后立即推进一次切换器；状态未变化时
     切换器自身跳过重绘，不会产生多余刷新，轮询任务保留作兜底（worker 自然退出等
     场景）。
  涉及 `module/webui/process_manager.py`、`module/webui/app_overview.py`，回归测试见
  `tests/test_process_manager.py`、`tests/test_webui_scheduler_switch.py`。

## 四、战役 / 低耗

- **紧急委托与三油低耗复用舰船装备**：初始旗舰等级检查标志改为类属性
  （`Ambush11._initial_flagship_check_done`），任务切换后再回来不重复换旗舰 /
  换装备。
- **修复主线打捞误读活动 PT**：GemsFarming 在普通战役页不读取活动 PT，避免把
  页面其他数字误识别成 1。
- **新增 `Commission.DetectShipDrop`（默认关闭）**：控制领取委托奖励后是否检测
  「获得舰船」画面；不做会掉落舰船的委托时可关闭，避免识别错误。其他场景
  （战斗结算等）的舰船检测不受影响。
- **委托收益数量识别**：奖励图标会伸入数量 OCR 区域，其白色纹理被误读成数字
  （心智单元 11 → 211、金币 71 → 2、特别兑换券 3 → 23 等）。逐步收敛为三条
  规则，战斗掉落统计路径行为不变：
  1. **连通域碎片过滤** —— 删掉「远离数字主体」的孤立图标碎块，保留靠近数字的
     小组件（被拆分的字形部件）；只删碎片像素、保留中灰像素，避免抹掉字形边缘
     造成 77→27 这类误读。
  2. **右侧数字簇聚类** —— 从最右数字出发按水平间隙（阈值 10px，数字间实测
     ≤4px）传递聚类，簇外的图标笔触删除；另删「悬在数字左侧、无水平重叠且
     面积 ≥20」的扁平横条（数字 1 左侧横条会让 1 被拼读成 7）。
  3. **两处兜底重试** —— 首轮读数超出物品上限时换用「抹灰版」图像（非保留像素
     全部置白）重试；首轮读数为 0 时用未裁剪图像重试（数量区域右边界可能正好
     切掉数字，而 0 是非法数量会作废整次结算）。
  4. **委托收入必须用最近邻放大**（`CommissionAmount.pre_process`）——
     **不能裁剪、也不能用双三次/线性插值**。委托页数字只有约 14px 高，其中
     `1` 的笔画宽约 1px；插值会把这条笔画和数字之间的空隙糊在一起，模型只能
     认出前一位，于是 `71` 被读成 `7`、`24` 被读成 `4`。原先这里写的是
     「裁剪 + 放大 2 倍」，那是**错的结论**（实测 2 倍双三次仍读不出 71）。
     现在的配置是 **3 倍最近邻放大 + 碎片过滤**，依据是 50 张留存结算截图的
     A/B 回归：相对旧配置只改动 5 张、且全部是修正，同时不引入新错误
     （关掉碎片过滤会多坏 1 张；2 倍最近邻仍读不出 71；3 倍与 4 倍等效，取小）。
     历史上因此产生的错值：`Oil: 7`（应 71）36 条、`Oil: 1` 4 条、
     `Coin: 7`（应 71）28 条、`Coin: 1` 9 条，集中在 2026-04 ~ 2026-08
     （那几个月没有留存截图，无法逐条复核，按决定未改动历史数据）。
  5. **碎片高度阈值抬到 10px**（3 倍放大域，约原图 3.3px）——心智单元等图标的
     白边会伸入数量区域，形成 6px 高的小块；按默认 6px 它们会被判为「大组件」
     当成数字（数字本身高约 42px），于是 `2` 被读成 `22`、`4` 被读成 `24`。
     抬高到 10px 后这些小块按碎片处理：离数字远则删除，属于字形本身的部件
     仍因贴近数字而保留。50 张留存委托截图回归：与数据库一致 **50/50**，
     并修好 09-15 16:18（24→4，库中本为 4）与 09-16 09:36（22→2，库中错值
     已修正，备份 `config/cl1_data.db.bak-chip-*`）两处。
  上限校验表补充模板名别名
  （`CognitiveChips` / `Gems` / `Cubes` / `Coins` / `OrdnanceTestingReportT4`）
  作为最终防线。
- **军械测试报告数量上限收紧到 5**：报告图标边缘的斑点很小又紧贴数字，
  碎片过滤按「保留抗锯齿小像素」的规则不会删，于是「1」被拼成 41/51/721
  （三张留存截图实测，抹灰版均读作 1）。上限由 50 收紧到 5（T3 一并补上）
  后，这类虚高读数会触发抹灰版兜底重试；上限 5 仍容纳真实的 1~5。
  因此记错的 2026-09-12（721）、09-13（51）、09-15（41）、09-16（41）
  四条记录已修正为 1（备份 `config/azurstats_local.db.bak-report-*` 等）；
  07-27、07-28 两条 `x7` 没有留存截图，按决定未改动。

## 五、大世界（侵蚀1 / 耄耋相接 / 明石）

- **统一显式调用 `OSMap.clear_question`**：`OperationSiren` 组合类里
  `self.clear_question()` 会按 MRO 解析到侵蚀1的多舰队版本（与上游注释声明的
  「仅当前舰队雷达，不切换 1~4 队」相反）。初始化首次自律寻敌、每日任务清理
  海域、耄耋相接检索等场景统一改成显式调用单舰队实现。
- **侵蚀一练级事件检索**：本地原四步顺序检索已在上游合入时**升级为上游的强制
  移动等级化模式**（配置 `ExecuteFixedPatrolScan`，0=关闭 / 1=效率 / 2=保守，
  旧布尔值自动迁移 True→2），行为等效且更完善。本地增强保留：强制移动沿用
  「候选落点 + 备用点返回」的两段式移动（合并进 `_move_fleet_to_patrol`）。
  清问号时**某支舰队走不到就改试下一支**：`OpsiHazard1Leveling.clear_question`
  按 `[主队, 其余队]` 顺序找第一支雷达上能看到问号的舰队去处理，原先失败即
  `return False`；但步数按舰队独立计算，舰队 2 步数耗尽走不到不代表 3/4 队也
  走不到，且外层「重扫 + 强制移动」会把同一目标重新报出来，形成约 30 秒一轮的
  空转（日志表现为固定用舰队 2 反复「舰队步数不足」→「前往问号3次尝试失败」）。
  现在改成失败后打一行「舰队 N 未能处理该问号，改试其他舰队」并换下一支，
  全部舰队都失败才返回 False。
- **耄耋相接事件检索**：先清主舰队周围问号 → 未解决则重扫 → 仍未解决再依次
  切换其他舰队。合入上游「自律寻敌后扫描雷达问号」「战后 debug 录像」
  （`_meow_debug_clip`）「战后效率模式强制移动」（`_meow_fixed_patrol_scan`）
  时保持单舰队行为，两个开关默认关闭，与本地检索逻辑并存（强制移动只在本地
  检索未解决事件时执行）。
- **明石商店一轮只进一次**：一次战役只处理一次，强制移动等后续环节不再重复
  进店；新一轮计划作战开始时重置标志（`_solved_map_event` / `is_akashi`）。
- **地图名称 OCR 稳定性**：等待 UI 过渡动画完成后再截图，避免读到不完整的海域
  名称（`module/os/map_operation.py`）。
- **智能调度+ 补黄币跳过重复的初始化自律寻敌**：黄币补充分支不再补执行初始化
  阶段跳过的首次自律寻敌（`handle_first_auto_search`），避免任务被打断后回到刚
  清理过的海域时重复全图重扫；黄币补充任务自身会执行战略搜索与事件检索，清理
  职责不缺失。
- **每日任务清理海域三步检索**：自律寻敌结束后按「清主舰队问号 → 地图重扫 →
  依次切 2/3/4 舰队清问号」执行，任一步解决事件即结束。
- **耄耋相接统计与月度收获**：
  - 数据收集表格新增「遇见明石次数 / 遇见明石概率 / 平均体力」三列，按侵蚀等级
    独立统计（`module/statistics/cl1_database.py` 的
    `increment_meow_akashi_encounter` / `add_meow_akashi_ap`），与侵蚀一统计
    互不干扰。
  - 收获区合并为**一个板块、一张表**（`app_stat_opsi_export.py`）：原先「本月
    耄耋相接收获」（当月掉落）与「耄耋相接收获」（累计平均值）是两张表、还叠了
    两层标题，现在只留「本月耄耋相接收获」一个标题（切到历史月份自动变成「历史
    耄耋相接收获（YYYY-MM）」），累计表已删除。
  - 累计表拆成两处显示：
    - 「上次记录时间」作为汇总行放在**标题与表格之间**（取各侵蚀等级里最新的
      一次记录时间），与主表汇总行同一形式；
    - 其余 5 列（有效战斗轮数 + 平均黄币 / 金菜 / 深渊 / 隐秘每轮）**追加到本月
      表右侧**，按侵蚀等级对齐取值；后 4 个平均值保留 **6 位小数**（数值本身很小，
      3 位时看不出差异）。该等级没有累计数据时这 5 格填 `-`。
  - 带「查看历史月份」（数据来源为本地掉落明细库中实际存在的月份）与「回到
    本月」按钮；切月份只重绘本月表，「上次记录时间」与右侧累计列不随月份变化。
  - 新增 i18n 键 `MeowLastRecord` / `MeowEffectiveRounds` / `MeowAvgOperationCoin`
    / `MeowAvgPlate` / `MeowAvgAbyssal` / `MeowAvgObscure`（五种语言）。原
    `MeowLootTitle`（外层板块标题）与 `NoMeowDataNotice`（累计表无数据提示）
    随本次改版失去引用，键保留未删。
  - **补充 11 个结算物品模板**（均在 `assets/stats/opsi_reward_items/`），修复
    各类「未知数字代号」与跨级误匹配：军用/工业级电子元件第 4~6 形态、B 型火药
    第 2 形态、HBX 混合火药第 2 形态、防空装备设计图 T5 第 2 形态、舰炮装备
    设计图 T4、特装原型、军械测试报告 T3 第 4~5 形态。
    注意 `OrdnanceTestingReportT3_4` 必须取自该结算第 2 排第 6 列（军械测试报告
    格），误取第 1 排第 3 列（合成弹性材料格）会导致后续 12 个结算的合成弹性
    材料被误判。
  - 仍未识别的历史物品只剩数字代号 61 / 62，无对应截图无法辨认，保留原样。
- **侵蚀一练级行动力开箱防溢出**（`avoid_ap_overflow`）：开工前要求行动力
  ≥120，但当前行动力在 **100-119 时直接开工、不开行动力箱**（不等自然恢复，
  也不开 100 箱造成溢出）；低于 100 时按 20→50→100 顺序开箱补齐，开箱后达到
  或超过 200 满值的箱子跳过。其余任务开箱行为不变。
- **大舰队弹窗优先关闭**：大舰队弹窗是模态弹窗会吃掉导航点击（表现为反复点击
  毫无反应），因此在任意页面都优先处理（`module/ui/ui.py` 的
  `handle_guild_popup_cancel`），且排在岛屿页面保护之前。

## 六、OCR 与设备选择

- **独显优先，避免绑定虚拟显示器适配器**：机器上多个虚拟显示器适配器
  （GameViewer / Virtual Display Driver / MuMu 虚拟屏）会各自枚举出一个
  DirectML 设备，且都镜像真实独显的名称与显存；上游 `_is_discrete_gpu()` 对
  缺失 `Discrete` 元数据的设备退化为「名字不像核显 + 显存够大即放行」，于是
  OCR 可能绑到会被热插拔的虚拟适配器，显示配置变化后会话失效，DirectML 抛
  本地化（GBK）错误消息被 onnxruntime 按 UTF-8 解码失败，最终以
  `UnicodeDecodeError` 掩盖真实错误并中断任务。
  修复：`module/ocr/windows_ml.py` 新增 `_device_priority()`，
  `_iter_preferred_devices()` 在同一 EP 候选内按「`Discrete=1` 优先 →
  `DxgiHighPerformanceIndex` 升序」排序，并逐条打印候选设备便于排查。排序只
  调整尝试顺序，虚拟适配器仍保留为后备，不影响无独显标记的机器。
  新增 `tests/test_ocr_device_priority.py` 覆盖该排序规则。

## 待补充（已在上游 diff 中但尚未归类）

`git diff wess09/master master` 涉及 82 个文件。以下文件有实质改动，但清单里
没有对应描述，需要确认归属：

| 文件 | 改动内容 | 归属 |
|---|---|---|
| `module/statistics/item.py` | `remove_small_fragments()` / `ocr_with_validation()` / `match_candidates()` / `predict()` | 四、数量 OCR；五、图纸等级（已描述，文件名可补） |
| `module/statistics/azurstats.py` | `get_meow_loot_monthly_totals()` / `get_meow_loot_available_months()` / `opsi_save_method()` / 未知物品截图保存 | 五、耄耋相接统计与月度收获 |
| `module/statistics/cl1_database.py` | 明石遭遇与行动力的增量写入（含异步版） | 五、耄耋相接统计表新增列 |
| `module/azur_stats/image/auto_search_reward.py` | 奖励页物品识别 | 四、数量 OCR |
| `module/statistics/opsi_runtime.py` | 明石遭遇记录 | 五、耄耋相接统计 |
| `.gitignore` | 新增 16 行忽略规则 | **未归类，待确认** |
| `module/config/config.py` | 岛屿任务半点对齐 | 三、调度（已描述） |
| `module/os_shop/shop.py` | 明石商店 | 五、明石去重（已描述） |
| `module/webui/app.py` | `/static/commission_rewards` 路由 | 一、委托收益截图（已描述） |

结论：只有 `.gitignore` 是**完全未记录**的改动，其余都已被现有条目覆盖，
只是条目里没点出文件名。

## 七、测试护栏

- **`tests/pywebio_stubs.py`：整体打桩 PyWebIO 输出函数**。
  PyWebIO 的输出函数（`put_html` / `put_row` / `put_scope` / `put_button` …）
  在**没有活动会话**时被调用会进入「脚本模式」—— PyWebIO 自己起一个服务器，
  并调用 `open_webbrowser_on_server_started` **用系统默认浏览器打开页面**：

  ```python
  # pywebio/platform/tornado.py
  async def open_webbrowser_on_server_started(host, port):
      url = 'http://%s:%s' % (host, port)
      threading.Thread(target=webbrowser.open, args=(url,), daemon=True).start()
  ```

  症状是跑单元测试时**凭空弹出「PyWebIO Application」标签页**（`127.0.0.1:<随机
  高位端口>`），而且服务不会退出、测试进程也可能跟着挂住。
  各渲染 harness 原先只挑自己用到的那几个函数打桩，**渲染路径新增一个输出调用
  （例如给图表标题加刷新按钮时新增 `put_scope` / `put_button` / `put_buttons`）
  就会重新开始弹窗，且测试结果完全看不出来**。现在统一用
  `stub_pywebio_output(模块名, capture=...)` 一次性打桩该模块暴露的全部输出
  函数；记录按 `setdefault` 语义保留**第一次**调用（面板 HTML 之后的
  `<style>` 注入不会顶掉它）。
  已接入：`test_webui_ap_chart_theme.py`（渲染 harness）、
  `test_webui_statistics_page.py`（两处 harness 补 `put_html`）。
  验证方式：跑测试时监控新出现的浏览器进程，修复前会抓到 Edge、修复后为 0。

- **第二道防线：`PYWEBIO_SCRIPT_MODE_PORT=0`**。只靠「各个 harness 记得打桩」是
  不够的 —— 2026-09-19 的排查里就漏了：临时基准脚本直接在无会话下调渲染路径，
  一次连开了 6 个标签页。看 Tornado 平台的开浏览器代码就知道为什么漏桩会弹窗，
  而且 `auto_open_webbrowser` 管不住它：

  ```python
  # pywebio/platform/tornado.py，脚本模式分支
  if "PYWEBIO_SCRIPT_MODE_PORT" not in os.environ:
      tornado.ioloop.IOLoop.current().spawn_callback(
          open_webbrowser_on_server_started, '127.0.0.1', port)
  ```

  所以现在在 `tests/pywebio_stubs.py` **模块顶层**就
  `os.environ.setdefault("PYWEBIO_SCRIPT_MODE_PORT", "0")`：导入它即生效。设成
  `0` 等价于原来「让系统分配端口」，只是跳过了开浏览器那一步，对功能没有影响。
  漏桩的 harness 顶多让脚本模式起个后台服务器，不会再往桌面上丢标签页。
  实测（打桩 `open_webbrowser_on_server_started` + 数浏览器进程）：加固前会命中
  `server_started 127.0.0.1 <随机端口>`，加固后 `OPENED []`、浏览器进程数不变。

## 八、界面技术栈决策（上游改 React 后仍保留旧界面）

上游已把整个 WebUI 换成 React 前端（`frontend/` + `module/api/`），并从依赖里移除
`pywebio`、删除 `module/webui/*` 与 `webapp/*`。本地决定**继续使用旧 PyWebIO 界面**，
做法是「保留界面 + 复用上游新运行时」，而不是把旧运行时整套复活：

- **界面本体**：`module/webui/app_*.py`、`widgets.py`、`base.py`、`api.py`、
  `webapp/`、`assets/gui/` 全部保留（含本文件第一章的全部界面定制）。
- **基础设施转发层**：上游把非界面基础设施搬到了 `module/runtime/`，因此
  `module/webui/{setting,process_manager,worker_registry,updater,config,mcp_auth,
  password_utils,remote_access,launcher,launcher_trust,deploy_settings,
  discord_presence,scheduler_stop}.py` 改为**转发层**（`__getattr__` 转发 +
  符号注入），保证旧界面与上游新 API 共用同一份 `State` / `ProcessManager`，
  不会出现两份互不可见的单例。
- **例外**：`module/webui/event_calculator.py` 保留旧版本体。上游的同名模块
  删掉了 `build_error_html` / `build_event_calculator_html` 等旧界面依赖的符号，
  转发会直接缺符号；它是无状态的渲染工具，两份并存无副作用。
- **入口**：`gui.py` 默认启动旧界面（`module.webui.app:app`）；设置环境变量
  `ALAS_WEBUI=react` 可切回上游新前端（此时才会构建 `frontend/`）。
- **依赖**：`pyproject.toml` 保留 `pywebio==1.7.1`（上游已移除）。其余被上游移除的
  依赖（aiofiles / chardet / watchdog / pygments / sse-starlette / wrapt / retrying 等）
  已确认无任何引用，不恢复。
- **代价（须知）**：这是一次有意的分叉。上游后续对 WebUI 的改动都会落在
  `module/api/` + `frontend/`，旧界面不会再获得上游的界面新功能；每次合入时
  `module/webui/*` 与 `webapp/*` 会持续以 modify/delete 冲突的形式出现，需按本节
  方式保留。
- **假 PIL 与测试**：旧界面在导入期把假 PIL 注入 `sys.modules`（为加快 WebUI 进程
  启动）。这在整套 `unittest discover` 里会污染后续需要真实 PIL 的用例，因此在
  `import_fake_pil_module()` 里加了「跑测试时不注入」的判断（`unittest` 已在
  `sys.modules` 时跳过，也可用 `ALAS_SKIP_FAKE_PIL=1`），生产路径不受影响。
- **测试打桩路径**：转发层不参与全局名查找，凡是对已迁移模块打桩的测试都要指向
  真实模块（如 `patch("module.runtime.process_manager....")`），
  打在 `module.webui.<同名模块>` 上不会生效。

### 上游改版后仍保留的行为差异（本次融合点）

- **短猫相接检索链**：保留本地「先清主队问号 → 重扫 → 切 2/3/4 队清问号」分步链；
  上游已删除该链、改由强制移动（`ExecuteFixedPatrolScan`，默认关）承担。融合为
  **互斥去重**：强制移动开启时不再自己切队扫雷达（避免同一批雷达连扫两遍），
  关闭时沿用本地分步链，默认行为与定制一致。
- **委托收益记录粒度**：保留本地「一个获得道具弹窗 = 一条记录 + 一张截图」；
  同时吸收上游的截图开关（`DropRecord.CommissionIncomeScreenshot`）、
  按天数清理（`drop_screenshot_retention_days`）与钻石委托分时长统计。
- **军械测试报告数量上限**：保留本地实测校准的上限 5（T3/T4 都有）；
  上游同项只覆盖 T4 且放宽到 50，会让 41 这类虚高读数漏过校验。
- **停止按钮卡顿修复**：本地对 `ProcessManager` 的日志线程退出信号 + 收尾进程后台
  回收，与上游新增的预览线程在同一文件共存。
- **掉落截图落盘与科研解析共存**（合入 54 提交时）：本地 `_drop_save_image`
  （只把结算页落盘）与上游新增的 `commit(analyze=)` / `DropImage.analyze`
  （科研掉落走分类自己的解析链路）在同一文件合并保留，科研记录不受截图筛选影响。
- **委托数量 OCR**：本地实测的「3 倍最近邻 + `fragment_min_height = 10`」完整保留，
  同时吸收上游在同一识别器上的一行修复（`GET_SHIP` 检测加 `offset=(20, 20)`）。
- **科研掉落统计**：上游新分类 `research`（`module/statistics/research_stats.py` +
  `assets/stats/research_items/`）整体吸收；旧界面里没有对应面板，不改动。

## 维护约定

- 每次新增定制修改并提交时，同步更新本文件对应章节。
- 若某项定制后来被上游合并或被回退，应从本文件移除并在提交说明中注明。
- 条目的「为什么改」比「改了哪些行」更重要；调参过程、回归数据等细节留在
  提交说明里，本文件只保留结论与关键决策。
- 引用其它条目时用条目名而不是编号（编号会随整理变化）。
